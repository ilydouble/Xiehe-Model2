# Findings & Decisions

## Requirements
- 2026-09-08：用户已完成第一批368张复核，要求只保留accepted，与第二批构建全新侧面脊柱Pose数据集；必须排除错配和重复，并更换旧密集polygon四角提取算法，禁止三角形退化，随后用于重新训练。
- 用户本轮写明目标为“C2和C7、T1-T13、L1-L5”，与先前23类C2-C7/T1-T12/L1-L5不同；构建前需通过数据与训练历史核对并确认是20类新口径还是“C2-C7”的连字符遗漏，同时确认无样本T13是否保留空类。
- 2026-09-08：用户发现yolo_corner中不止个别旧C7-L5标注疑似错配，要求全量统计；确定错配图像不能只补C2-C6，而要重新补全全部23类标注。
- 2026-09-08：用户将补标范围最终限定为C2-C6。C7-L5即使原始缺级也不得由模型补齐；考虑真实个体可能缺少某级椎体，不依据原始缺级自动排除，所有384张由用户在可视化包中人工接受/拒绝。
- C2贴到或越过图像边界的样本不得静默纳入：可视化、CSV和HTML必须显式提示并支持筛选，但最终是否排除仍由用户决定。
- 2026-09-08：用户已将第二批23类侧面脊柱模型拷回本机，要求下一步为第一批 `/Volumes/E/spine_data/LAT202511` 补标注。
- 补标注必须保留E盘原图和原LabelMe JSON，先将候选结果写入workspace独立目录，人工复核后再决定合并。
- 第一批存在大量左右黑边，推理应至少比较原图和安全去连续黑边视图，裁剪视图预测必须精确映射回原图坐标。
- 用户已授权处理侧面三关键点联合数据集：参照正面模型把聚焦裁剪视图加入训练，同时必须避免患者或同源派生图跨train/val/test泄漏。
- 泄漏边界固定为先使用既有患者级split，再只从 `images/train` 生成派生ROI；train混合原图和ROI，val/test不生成也不引用GT裁剪视图。
- 分析 `/Volumes/E/spine_data/20260903-侧面数据第二批` 中的侧面数据。
- 数据标注格式为 LabelMe。
- 默认分析范围：规模、配对完整性、类别与 shape 分布、图像/标注几何统计、异常检查、抽样视觉质检。
- 不改动原始数据。
- 评估本批 706 例能否与仓库中的既有侧面数据统一成一个大数据集；当前仅分析和设计合并口径，不实际复制或改写数据。
- 用户已要求实际把第二批转换到 `datasets/` 下，并提供稍后可在 AutoDL 执行的训练脚本。
- 当前构建目标固定为 23 类椎体 Pose：C2-C7、T1-T12、L1-L5；每个目标统一为 4 个有序角点。T13 保留为未来扩类需求，但第二批没有 T13 样本；S1 和 CFH 不进入本模型。
- 用户要求继续构建两批合并的骨盆关键点数据集，并提供在23类侧面脊柱模型训练完成后使用的独立训练脚本。
- 联合骨盆目标固定为一个 `pelvis` 对象的3个关键点：CFH、S1图像左端点、S1图像右端点。
- 用户要求补充第二批706张原图的图像层面分析，至少覆盖分辨率、宽高比例以及上下左右黑边，并形成可复现统计与训练建议。
- 本机无Pillow/OpenCV/NumPy，FFmpeg可用；黑边扫描可让FFmpeg将每张PNG按宽256等比例缩小为8位灰度PGM，再用标准库计算连续边缘近黑行列，避免新增依赖。
- 黑边自动判定固定为：缩小灰度图中像素值≤5视为近黑；从每侧连续统计近黑像素占该行/列≥98%的边缘带。任何像素带单独记录，单侧≥1%才定义为明显黑边，上下或左右合计≥10%定义为严重黑边。
- 12个来源校准样本中多数没有连续黑边，FZBW/FZFH/KWSY仅见约0.4%的极薄上下黑线；抽样LFDS上下各约11.2%为连续纯黑带，证明需要区分轻微边线和明显黑边。
- 全量706张均为纵向8-bit PNG：391张灰度、315张RGBA；共有292种尺寸，384张（54.4%）是1536×4352。
- 宽度范围984-1536（中位1536），高度2924-4352（中位4352），像素量3.39-6.68MP（中位6.68MP）；宽高比范围0.238-0.512，中位0.35294，499/706（70.7%）落在0.35-<0.40。
- 黑边扫描检测到144张至少有连续边缘黑线，但只有43张（6.1%）单侧达到≥1%的明显黑边，26张（3.7%）达到上下或左右合计≥10%的严重标准。
- 黑边几乎全部是上下对称：上/下分别143张检测到、42张明显；左侧仅1张约1.17%，右侧0张。最大样本上下分别约16.39%和16.53%，合计约32.9%。
- 明显黑边高度集中于来源：LFDS 15/21、GZLL 11/30、LWSY 10/80、KWSY 3/11；FQSY、LFPY、PZNS、PZSW、SZSH、PYGZ均为0。来源与颜色模式、尺寸和黑边因此存在耦合。
- 视觉复核最大LFDS、LWSY、唯一左边框XTZY和无边框FQSY样本，自动判断与肉眼一致：大黑边是图像画布上下的实质空白，不是人体周围正常暗背景；唯一左边框很窄，无边框样本内容到达画布边缘。
- 517/706（73.2%）宽度为1536，384/706（54.4%）高度为4352，且这384张全部为1536×4352；其余322张形成291种多样尺寸，反映不同来源的裁切/导出策略。
- 按上下/左右合计黑边占比：562张无连续黑带，102张<2%，6张2-<5%，10张5-<10%，9张10-<20%，17张≥20%。明显黑边43张中41张为RGBA、2张为灰度。
- 对全部315张RGBA逐图抽取并缩小alpha通道复核，315张均为全255、完全不透明；RGBA只是冗余通道，检测到的黑边来自真实RGB灰度像素，不是透明区域合成效果。
- 第二次在独立临时目录全量扫描706张后，逐图CSV和汇总JSON均与首次输出逐字节一致，统计具有确定性和可复现性。
- 训练建议是先保留当前数据完成基线：使用保持比例的letterbox与rect批次，不做固定方形拉伸。若来源分层误差提示域偏差，再对单侧≥1%的43张明确黑边做裁剪并同步坐标，开展对照实验；不可只裁图不改标签。
- 联合转换器把三点外接范围在每轴增加20%边距，并设置图像尺寸2%的最小边距；该框只用于YOLO Pose对象定位，关键点坐标保持医学标注本身。
- `flip_idx=[0,2,1]`：水平翻转后CFH索引不变，S1图像左/右端点互换；因此训练可以安全启用水平翻转。
- 为避免来源占比偶然失衡，旧批和新批分别按患者作确定性80/10/10拆分后再合并；患者group id含批次命名空间。
- 旧批有5例同时存在唯一合法S1 line和额外S1 circle；三点任务应采用line端点并忽略辅助circle。纳入这5例后，旧批候选仍与早期复核的335例一致。
- 联合数据集最终纳入1,028张：旧批335个有效候选去掉1张完全重复后为334张，新批694张；对应1,023个带批次命名空间的患者组。
- 联合split为824 train、102 val、102 test；旧批在三者中为268/33/33，新批为556/69/69，两批均覆盖所有split且无患者交叉。
- 1,028行标签中569个CFH来自直接point、459个来自FH-1/FH-2中点；全量14字段、类别0、坐标范围、bbox和可见性检查均通过。
- 骨盆训练入口默认从 `yolo11m-pose.pt` 初始化，也可传入刚训练的23类侧面模型 `best.pt`；后者可迁移侧位X线骨干特征，但1类/3点Pose head会按新data.yaml重建，不能直接沿用旧head推理。
- 三点任务仍保留 `rect=True`、关闭mosaic/mixup/copy-paste、只做轻微旋转平移缩放和亮度变化；水平翻转因data.yaml已有正确flip_idx而可设为0.5。
- 仓库训练目录职责明确：`3-model_training` 放脊柱椎体Pose，`4-model_training_CFH` 放CFH/骨盆相关训练；联合骨盆三关键点脚本应位于后者。

## Research Findings
- 用户指定的实际补标对象是 `datasets/yolo_corner`：它是18类、每个椎体4关键点的YOLO Pose数据集，包含368张图与368份标签，split为294 train、37 val、37 test；类别为C7、L1-L5、T1-T12，不含C2-C6。
- 先前严格复核包覆盖原始LAT中384个同stem PNG/JSON配对，但与yolo_corner只有358张文件名交集：包里多了26张不属于yolo_corner的图，同时漏掉yolo_corner中的10张图。因此不能把该384张包当作yolo_corner的完整复核包。
- 10张漏图均来自原始LAT中没有同stem JSON、却被旧转换器纳入yolo_corner的图：train 9张、val 1张、test 0张。旧转换器明确取患者目录的第一份JSON并套到目录内所有PNG，这10张的现有18类YOLO标签存在错配风险。
- yolo_corner标签格式本身合法：每行17字段（class+bbox+4×x/y/visibility）。train中293张为18对象、1张为11对象；val全部18对象；test中36张为18对象、1张为11对象。11对象不能仅凭数量判错，需要结合视野和原始标注人工判断。
- 两个11对象样本均属于WANG_YANG_DI同一患者，分别位于train和test，缺少C7、T1-T4、T7、T8；这同时说明现有split存在患者泄漏，但按用户口径不能仅因缺级判定样本不合格。
- 前一版复核图的青色基线来自原始LabelMe密集polygon；yolo_corner的真实训练标签则由旧转换器选取轮廓最远4点并写成YOLO Pose。即使358张图像重合，前一版也没有忠实展示实际训练标签，因此必须按yolo_corner标签重新渲染。
- 正确的纠正方案应保留 `datasets/yolo_corner` 只读，另建复核/重建目录：以368张现有图和YOLO标签为基线，只叠加C2-C6模型候选；对10张无同stem JSON、1组精确重复和5个跨split患者显式警告，最终根据人工结论按患者重新拆分，不能直接覆盖现有split。
- 纠正版12张真实试包审计通过。AN_JIA_YUE显示青色基线为yolo_corner实际四角点而非LabelMe密集轮廓，C2-C6五个high候选顺序清楚；BEN_ZI_YUAN的C2越过/贴住上缘并正确显示0.0px触边警告，C6与青色C7的衔接可在局部图直接核对。
- 首轮368张正式生成完成：358张复用候选、10张补推理；审计为368/368通过。风险计数为旧标注错配10张、跨split患者所涉图像8张、精确重复2张、基线缺级2张，均进入HTML筛选且不自动排除。
- 10张补推理中8张得到完整C2-C6；LIANG_TIAN只得到C2，ZHAO_YI_XUAN的0101图缺C4/C6。推理时出现2次CPU NMS时间警告，为避免候选因大量低置信框被截断，纠正版将NMS前阈值直接设为最终candidate_conf=0.03后重新核对这10张。
- 用0.03阈值独立重跑10张后不再出现NMS警告，10张的候选类别集合与正式包完全相同；重新渲染有9张JPG逐字节一致，仅CUI_YI_NING出现约0.45px的C2边距与1px裁剪框浮动，旧/新图肉眼叠加位置一致，证明先前NMS警告没有改变可供人工判断的候选结果。
- 正式高风险图抽查进一步证明提示必要：LIANG_TIAN的无同stem JSON图实际为正位片，却带有侧位yolo_corner基线，C2模型候选也落在胸椎区，应人工拒绝；WANG_YANG_DI图为侧位且C2-C6候选合理，但基线缺C7/T1-T4/T7/T8并存在患者跨split，不能因缺级自动排除，需人工决定并在最终重拆时消除泄漏。
- 正式包又抽查精确重复与test样本：WANG_YANG重复图没有任何C2-C6候选，页面同时显示“精确重复图”和五类无候选；test中的BAO_QING显示C2触边且缺C5/C6，青色18类四角点与候选颜色区分清楚。
- 通过原始LAT患者目录ID重新映射368张后，准确患者数为353，跨split为4个患者/8张图；同名但不同ID的两位YANG_YI不属于泄漏。正式包的`patient_leak_count=8`使用这一精确口径。
- 全量反向重放旧转换器后确认：353/368张YOLO标签精确来自本图同stem JSON，15张精确来自另一JSON。15张中WANG_YANG一张与来源图逐字节相同，应去重；其余14张不是同一图，必须补全全部23类，分布为train 13、val 1，涉及11位患者。
- 原先仅以“是否存在同stem JSON”标记10张会漏掉4张：它们虽然有自己的JSON，但旧转换器固定取患者目录第一份JSON，现有YOLO数值可精确证明标签来自别的JSON。当前E盘复核包的旧标签错配筛选因此不能作为最终口径，应暂停并重建。
- 第一批384张配对图中，原始C7-L5恰好各一个polygon的有357张，27张存在至少一级缺失；按用户最新口径只记录、不自动排除。
- 仅看C2-C6候选，354张五级齐全，30张至少缺一级；有C2候选的366张中33张四角点到图像边界≤1.01px，48张到边界≤图像短边0.5%。新版包将前者标为“触边”、后15张标为“近边”，均交人工判断。
- 新版12张真实试包的视觉检查确认严格口径生效：BAO_QING仅叠加C2-C4，缺C5/C6且C2顶部0px触边被红色提示；CHEN_CHEN原始缺C7、T1-T5时只显示橙色信息提示，模型没有补画这些级别，仍仅叠加C2-C6。
- E盘正式包抽查确认风险类别可读：BU_JUN_QING的C2距顶部约9.7px，被标为“近边”，局部图能看到C2几乎贴住画布；CHEN_MAN_TING没有强行补C2，顶部明确显示“C2无候选”，仅叠加C3-C6供人工接受或拒绝。
- 正式包进一步抽查C2触边与安全样本：BAO_QING的C2被画布上缘截断并显示0.0px触边、同时保留C5/C6无候选提示；AN_JIA_YUE的C2-C6五个高置信候选完整且边界安全。四类风险代表图的颜色、顺序和警示均符合复核设计。
- 正式包独立计数得到C2/C3/C4/C5/C6候选分别366/377/366/370/370个，共1,849个；384个relative_image和preview均唯一，新增类别集合严格限制为C2-C6，意外类别0。
- 全量候选输出位于 `datasets/lateral_first_batch_pseudolabel_23cls`：384份严格配对样本全部处理，新增1,942个缺失polygon，100个类别无候选；质量分级high 1,547、medium 231、low 164，所有等级均需人工复核。
- C2-C6中，354/384张五类都有候选，30张至少缺一类；6张完全没有C2-C6候选：WANG_YANG两张、XU_LONG_YUE、YANG_HUI_JUN、YU_CHU_YI、ZHAO_WEN_QI，应优先人工补画。
- 独立audit-only全量通过：manifest 384行、review queue 2,042行、原shape前缀保持、384个源图符号链接、所有新增四点/坐标/旗标和统计闭环均无错误。
- `labelme_review/` 已生成384个扁平JSON和384个指向E盘原图的同名符号链接，且文件名无冲突；可直接把这一个目录交给LabelMe连续翻阅。
- 12张正式试标（严重黑边4、轻中黑边4、无黑边4）共新增62个缺失polygon：high 48、medium 6、low 8；另有5个类别无可靠候选。逐张可视化显示大多数C2-C6落在正确椎体，BAO_QING缺C5/C6、SHI_HONG_YI缺C4-C6等难例被留为unresolved而没有强行补齐。
- 试标输出完整性复核通过：12份输出JSON均保持原始shapes为完全相等的前缀，新增shape全为polygon且带needs_review；图像均为指向E盘原图的符号链接；新增四点全部位于原图边界内，错误0。
- 当前Miniconda的torch报告MPS unavailable；即便参数写mps，Ultralytics实际predictor和模型参数仍落在CPU。因此全量本机推理预计需要约15-20分钟，脚本默认auto选择CPU是正确行为。
- 第一批有404张PNG、384份同stem JSON和20张无同stem JSON的图片；首轮只处理384个严格配对样本，20张图片单独列入排除清单，避免误用同患者目录中的其他JSON。
- 新批692个完整样本显示：C2顶部相对C7-L5已知脊柱高度的扩展比例中位0.186、P95 0.207、最大0.226；但把老图裁成局部颈椎块会失去全脊柱上下文并导致模型几乎不检出颈椎，因此不采用局部颈椎推理。
- 对第一批30张分层样本（严重黑边10、轻中黑边10、无黑边10）以conf>=0.05试验原图、连续黑边裁剪图和宽高比0.353的全高视图。C2-C6每图完整候选分别为：原图26-29/30、去黑边26-29/30、训练比例视图26-29/30；三者没有绝对赢家，应按跨视图一致性选候选。
- 同一30张样本用已有C7-L5 polygon作只读代理真值，预测框IoU>=0.1的召回在conf>=0.05时仅为：原图59.0%、去黑边59.4%、训练比例视图60.0%。这证明第一批域差异明显，不能把伪标签无审核自动视为真值；所有新增polygon都必须进入LabelMe人工复核。
- 严重黑边QIN_HE代表图上，原图C2-C6最高置信度约0.42/0.30/0.18/0.29/0.045，去黑边约0.51/0.39/0.24/0.18/0.14；裁黑边有帮助但没有消除域差异。第二批验证代表图则各椎体多为0.7-0.9，差距清晰。
- 2026-09-08定位到用户新回传权重 \`3-model_training/runs/pose/yolo11l_lateral_23cls_best/weights/best.pt\`（52,991,338 bytes，文件时间2026-09-08 00:36）；Ultralytics成功加载并确认task=pose、names=C2-C7/T1-T12/L1-L5、kpt_shape=[4,3]。
- 回传run的 \`args.yaml\` 确认使用YOLO11l-Pose、300 epochs目标、imgsz 1280、batch 2、rect=true及无mosaic/mixup；结果表有162轮，Pose mAP50-95最高0.92709（epoch 112），符合早停后回传best权重的特征。
- 本机Homebrew Python缺少推理依赖，但 \`/opt/miniconda3/bin/python3\` 已有torch 2.8.0、ultralytics 8.3.232、OpenCV 4.12.0、Pillow 11.3.0、NumPy 2.2.6，可直接执行本机试标。
- 第一批共384份可解析JSON。C3-C6均为0；C2有18个circle，已知语义是股骨头辅助圆而不是颈椎；C7-T12/L1-L5大多为已有polygon，但各类有4-13份缺失。补标脚本应只填充缺失的目标polygon，不替换任何原shape；新的颈椎C2 polygon可与旧C2 circle同时保留，下游按shape_type区分。
- 最终独立泄漏审计通过：源数据1,028张/1,023患者组，ROI 824张/819患者组；源患者跨split=0、源图跨split精确重复=0、ROI非train患者=0、ROI与val/test原图精确相同=0，错误列表为空。
- `scripts/build_pelvis_roi_views.py --audit-only` 会重新计算全部源/ROI哈希并写 `datasets/yolo_pelvis_3kpt_roi_views/leakage_audit.json`，可在上传AutoDL后再次运行确认传输未破坏数据边界。
- 新混合YAML训练规模为train 1,648视图（824原图+824 ROI）、val 102原图、test 102原图；训练校验器从源manifest确认总患者仍为1,023，ROI仅对应819个train患者。
- 三点训练shell默认使用ROI混合YAML，仍可通过 `--full-frame-only` 回到824张原图train以开展同split A/B；两条路径共享完全相同的102 val和102 test。
- 正式ROI数据集已生成，大小约1.5GB：824张train派生图与824份标签一一配对，819个患者组，来源保持旧268/新556；val/test派生目录不存在。
- 全量824张ROI的保存尺寸与manifest裁剪尺寸一致，源图/输出图/输出标签SHA-256均匹配manifest；全部标签为1行14字段、class 0、三点可见且所有坐标在[0,1]，独立检查错误数0。
- 边距全量对照结果：2.25/2.5/2.75的ROI面积中位数分别44.60%/50.06%/54.75%；选2.5作为默认值，使侧面三点ROI接近正面约53%的训练视图尺度，同时避免2.75进一步保留过多空画布。
- 视觉查看旧批最小24.83%与中位48.63%、新批最小24.72%与中位50.85%的代表ROI：四张均保留骨盆目标、腰骶结构和足够身体上下文；新批最小ROI仍覆盖较长脊柱，说明统一规则没有把窄长第二批裁成过紧局部图。
- 初版真实dry-run发现 `old__DAI_WEI_ZHONG...` 的六位小数YOLO框底边比原图多约0.0027 px；这是序列化舍入而非标注实质越界。ROI变换现先把源bbox像素边界裁到原图范围，并用该真实数值加入回归测试。
- 修正后824张train全部可规划ROI；默认边距2.0的实际ROI面积分布为最小18.36%、P10 28.54%、中位38.80%、P90 54.38%、最大83.39%。实际中位数低于理论49%，主要因骨盆位于图像下部、ROI底边受画布截断。
- 三点ROI最终方案固定为：对象框每侧外扩2.5倍（总目标宽高约6倍），确定性平移扰动5%、尺度扰动10%、关键点/原框安全余量3%；实际ROI面积中位数50.06%，接近正面约53%的范围，而非生成仅占约4%的过紧骨盆切片。
- 输出采用独立 `datasets/yolo_pelvis_3kpt_roi_views`，只含 `images/train` 与 `labels/train`；混合YAML同时引用原数据train和ROI train，val/test只引用原数据，原联合数据集及split不改写。
- 当前训练校验器只理解单一数据根目录，后续需在不依赖PyYAML的dry-run路径中显式识别混合配置和ROI manifest，且把824个派生视图计为train视图而不是患者或新病例。
- 三关键点现有对象框很小：train中bbox归一化宽/高中位数约0.117/0.167，面积中位数约2.01%。若直接照搬正面20%外扩，ROI面积中位数仅约3.93%，对骨盆任务过紧且与正面ROI尺度不可比。
- 正面六点和Corner的真实ROI面积中位数分别约52.56%和53.26%。为让侧面三点ROI保留相近上下文，三点任务应使用更大的相对边距；以对象框每侧外扩约2倍为起点，理论ROI面积中位数约49.13%，再通过真实边界裁剪统计校准。
- 侧面train来源保持为旧批268张、新批556张；两批原始画布宽高比中位数约0.560和0.353。ROI生成必须对两批使用相同几何规则，不能按来源设置不同裁剪策略，否则会强化来源特征。
- 现有 `datasets/yolo_pelvis_3kpt_all/data.yaml` 仅引用原始 `images/train|val|test`；训练校验器假设三个split都位于同一数据根目录，若增加独立ROI目录，需要扩展为可解析YAML中多train路径并同时校验派生manifest。
- 联合数据集manifest逐图保存 `batch、patient_id、group_id、split、image、label`，可以把每个ROI精确追溯到原train记录，并独立证明其源患者未出现在val/test。
- 当前环境没有Pillow，但FFmpeg可用且已用于图像全量扫描；ROI生成器可用标准库解析PNG尺寸、用FFmpeg无损重编码裁剪，避免为了本地构建新增图像库依赖。
- 正面模型实际采用的不是简单纯黑阈值裁边，而是由YOLO bbox与可见关键点并集确定ROI，外扩20%上下文并加入确定性5%平移、10%尺度扰动；每张train原图生成一张ROI，manifest记录源路径与哈希，val/test保持原图。
- 侧面三关键点数据现有患者隔离split为824/102/102，且两批来源在三个split中均有覆盖；本轮应保持现有split完全不变，派生图数量不应被表述为新增独立病例。
- 当前两套Pose训练入口均使用 `imgsz=1280`、`rect=True` 并关闭mosaic/mixup；这能保持纵横比并按相近比例组batch，但不会消除第一批左右空画布造成的有效解剖区域缩小。
- 若只按检测到的连续黑带裁边，第一批裁后宽高比中位数可从0.560降至0.476，单图保留面积中位数92.97%、均值91.83%；第二批裁后宽高比中位数仅从0.353变为0.356，保留面积中位数100%、均值98.99%。第一批裁边后仍比第二批更宽，说明除黑边外还存在真实构图/视野差异。
- 第一批404张PNG全部可解码，均为8-bit灰度、纵向图像，但有398种分辨率，几乎每张导出尺寸都不同；第二批706张有292种分辨率，且384张固定为1536×4352。
- 第一批宽度中位数3006 px、高度中位数5243.5 px、像素量中位数15.78MP；第二批分别1536 px、4352 px、6.68MP。按中位数比较，第一批约宽95.7%、高20.5%、像素量高136.0%。
- 第一批宽高比中位数0.560，第二批0.353，第一批约高58.7%；第一批309/404（76.5%）宽高比≥0.45，而第二批仅23/706（3.3%），说明差异不只是分辨率，画布构图也明显不同。
- 同一黑边判定口径下，第一批251/404检测到连续近黑边线，249/404（61.6%）有任一侧≥1%的明显黑边，192/404（47.5%）左右合计≥10%的严重黑边；384张有同stem JSON的子集中分别为244张明显、189张严重，结论不受20张无JSON图像影响。
- 第二批对应比例为144/706检测到边线、43/706（6.1%）明显、26/706（3.7%）严重。因此第一批的明显黑边率约为第二批的10.1倍，严重黑边率约为12.9倍。
- 第一批黑边方向与第二批相反：第一批顶部/底部均0，左侧187张明显、右侧247张明显；第二批顶部和底部各42张明显，左侧仅1张、右侧0张。
- 第一批总边带占比：153张为0，33张<2%，12张2%-<5%，14张5%-<10%，182张10%-<20%，10张≥20%；第二批分别562、102、6、10、9、17张。
- 第一批极端样本最大左右合计约29.3%（QIN_HE）和28.9%（LA_QIAN），最大单侧为右27.7%（CHEN_QING_RUI）或左21.9%（LA_QIAN）；视觉查看确认均是真实连续黑色空画布，而不是算法误把解剖区域判成黑边。
- “无连续黑边”只表示最外缘没有满足整行/整列98%近黑的连续带，并不等于X线影像背景中完全不存在黑色区域；本比较关注的是可被裁除的画布边带。
- 现有图像外观分析器只搜索源目录顶层PNG，不能覆盖第一批患者子目录结构；本轮将文件发现改为递归，并在逐图结果中保留相对路径，黑边计算本身和第二批阈值不变。
- 当前新增问题是比较第一批 `/Volumes/E/spine_data/LAT202511` 与第二批原始侧位图像的分辨率、宽高比和黑边差异；比较必须复用第二批的同一像素判定口径，避免阈值不一致造成假差异。
- 第一批比较范围采用原始目录中的全部404张PNG（含20张无同stem JSON的图像）；这回答的是原始图像域差异，而不是仅限最终训练入选样本。
- 第一批原始数据现位于 `/Volumes/E/spine_data/LAT202511`；复核得到384个JSON、404张PNG、370个CFH point、345个S1 line，335个JSON同时具备直接CFH和有效S1 line，S2为0。
- 第二批有694例同时具备可统一的股骨头中心与S1 line；其中FH-1/FH-2将取中点，直接CFH保持原坐标。两批原始候选合计1,029例，最终规模需扣除严格配对失败与精确重复。
- 严格同目录同stem配对得到旧批384对、新批706对；旧批额外20张无JSON的PNG不会进入联合数据集。
- 抽查两批确认S1 line均是两个像素坐标端点，CFH/FH均为单点；统一对象框可由CFH与S1两端点的外接范围加固定比例边距得到，避免不同批次依赖椎体数量产生框尺度偏差。
- 仓库 `.gitignore` 已整体忽略 `datasets/`，因此约 2.2 GB 的生成训练数据不会误入 Git；转换器、测试、训练脚本和说明应单独提交。
- 现有 `3-model_training/train_corner.py` 固定读取旧 18 类配置体系，且默认启用水平翻转；新 23 类数据需要独立训练入口，并应明确四角点 `flip_idx` 或关闭水平翻转，避免关键点语义错位。
- 现有新批图像为灰度与 RGBA 混合，但 Ultralytics/OpenCV 默认以三通道读取 PNG；本地构建可保留无损原图，避免重复编码，同时在转换报告中记录原始通道差异。
- 新转换器采用无额外依赖的 PNG/JSON 严格配对，按患者 ID 分组进行确定性 80/10/10 拆分；四角统一为左上、右上、右下、左下，水平翻转对应 `flip_idx=[1,0,3,2]`。
- 默认人工复核表隔离 8 个已确认漏标、内部缺层或 T1/T2 疑似互换样本；轻微越界 C2 坐标在生成标签时裁剪到合法图像范围，源数据保持不变。
- 已生成 `datasets/yolo_lateral_20260903_23cls`，大小约 2.0 GiB；纳入 698 张图像/697 名患者，train/val/test 分别为 558/70/70 张，患者数为 557/70/70，无患者跨 split。
- 生成数据共有 16,032 个椎体对象，23 个类别 ID 全部出现；全量校验确认每行 17 个字段、4 个关键点均可见、所有坐标在 `[0,1]`、图像标签严格配对。
- 隔离8例后，C2/C7分别有691/697个对象；T4-L5每类均为698个对象，少量缺少上颈椎的保留样本均为视野边缘截断。
- 本地存在 `6-app_backend/models/corner_model.pt` 可作为迁移学习起点；其旧18类检测头与新23类不匹配，Ultralytics训练时会保留可迁移骨干参数并重建/重训新任务头，不能把旧权重直接当23类推理模型使用。
- 再次用 `git check-ignore` 确认生成数据集整体由 `.gitignore: datasets/` 排除；AutoDL 部署时必须单独上传该目录，不能只依赖 Git 拉取代码。
- 新训练入口默认 YOLO11m Pose、1280长边、batch 4、200 epochs、矩形批次、无 mosaic、轻微亮度/旋转/平移/缩放及带正确 `flip_idx` 的水平翻转；高分辨率解码缓存默认关闭，避免AutoDL额外产生十几GB缓存。
- 训练脚本在导入Ultralytics或启动GPU前全量校验数据；真实数据 dry-run 已正确识别558/70/70张和16,032个对象。
- 兼容性核对中的“旧侧面数据”按仓库现状定义为 `datasets/LAT202511`。
- 旧数据为患者子目录结构：404 张 PNG、405 个 DICOM、384 个 LabelMe JSON；384 对 PNG/JSON 可按同目录同 stem 配对，另有 20 张 PNG 没有 JSON。新批次则是单层平铺的 706 对 PNG/JSON，因此物理目录不能直接拼接，需要生成统一清单或规范化目录。
- 旧数据 404 张 PNG 全为 8-bit 灰度；新数据 391 张灰度、315 张 RGBA。合并前必须显式统一通道。
- 旧数据有 384 个有效标注 JSON、7,598 个 shapes：6,834 polygon、370 point、345 line、49 circle；370/384 有单点 CFH，14 例没有股骨头点。新批次则有 459 例 FH 双点体系，因此股骨头语义是合并的首要冲突。
- 旧数据的常规椎体 polygon 是密集轮廓（例如每个 C7/L1 常有数十个点），新数据每个椎体 polygon 固定为 4 个角点。若最终任务是现有 YOLO pose 四角点，旧数据需要经“从轮廓选 4 点”的转换，新数据可直接使用 4 点；两者可以落到同一训练目标，但必须用同一角点排序规则复核一致性。
- 旧数据还有少量扩展/变异标签：C1 circle 26 例、C2 circle 18 例、T13 polygon 3 例、L6 polygon 8 例；其中旧 `C2` 是 circle，而新 `C2` 是椎体四点 polygon，标签名相同但 shape/语义不同，绝不能未经重命名或过滤直接合并。
- 抽查旧 C2 circle 的坐标显示它位于骨盆/股骨头区域，并非颈椎 C2；同一 JSON 还包含 C1 circle 与 CFH point。这进一步确认旧 C1/C2 是股骨头相关辅助标记，与新批次 C2-C6 颈椎 polygon 不同义。
- 旧数据 S1 表示也不统一：345 line、31 polygon、5 circle；新数据 S1 全部为 line。若当前训练仍排除 S1，则可统一忽略；若要训练 S1，需先统一表示。
- 旧数据共有 391 个患者目录，当前 `training_samples.json` 选出 353 个有效训练样本，排除 12 个无标注和 26 个标注不完整样本；因此若沿用现有 18 类椎体任务，理论合并规模约为 353+新批清洗后样本，而不是简单按 404+706 计算。
- 旧数据内部存在 1 组完全相同 PNG（同一患者 WANG_YANG 的两个文件名）；合并和拆分前需按图像哈希去重，且同患者全部进入同一 split。
- 经过“文件大小交集预筛 + SHA-256 候选哈希”，新旧两批没有完全相同的 PNG；两批甚至没有相同字节大小的 PNG。该结论只排除字节级重复，不能排除重新导出/缩放后的近重复影像。
- 按当前模型的共同 18 类（C7、T1-T12、L1-L5）计算，新批有 700/706 例完整，仅 6 例缺少至少一个共同类别。
- 当前 `datasets/yolo_corner` 配置记录旧 353 个患者样本产生 368 张图像，现有 split 为 train 294、val 37、test 37。患者目录数与图像数不一致，合并重拆时必须以患者为 group，不能沿用纯图像随机拆分。
- 旧转换器对每个患者目录只读取第一个 JSON，却把该标注应用到目录内所有 PNG；旧数据至少有多个含 2-4 张 PNG/JSON 的目录。因此重新构建大数据集时应改为按同 stem 严格配对，否则可能把一张片子的标注复制到同患者另一张片子上。
- 现有 `yolo_corner` 的368张图可精确映射回353个原始患者目录，其中4个患者跨越split：DE_JI_BAI_MA、FU_YI_YAN、GUO_ZI_YAN、WANG_YANG_DI。早期仅按文件名前缀曾把两个不同患者目录的同名YANG_YI误判为同一患者；以原始目录ID复核后已纠正。这4组仍构成患者级数据泄漏，最终必须按患者重拆。
- 旧数据有 18 例同时标注骨盆区 C1 circle、C2 circle 与 CFH point；C1/C2 圆心中点到 CFH 的距离中位数仅 5.88 px、最大 10.87 px（分别约占图像对角线 0.12% 和 0.24%）。这为把新批 `FH-1`/`FH-2` 中点统一成 `CFH` 提供了强证据，但正式转换前仍应确认新标签确实代表左右股骨头中心。
- 旧数据的完全重复 WANG_YANG 两张 PNG 当前都在 train，因此没有跨 split 泄漏，但会造成训练重复加权；重建时保留一张即可。
- 新批 CFH 与 FH 双点中点的归一化位置分布高度一致：x 中位数分别 0.4529 与 0.4679，y 中位数分别 0.7791 与 0.7719；结合旧数据 C1/C2 中点验证，`FH-1/FH-2 → midpoint CFH` 是合理的统一候选规则。
- 新批 706 张图像对应 705 个可解析患者 ID；仅 `XTZY2410P000605` 有两张不同时间/尺寸影像（141853 与 141932），两张均有完整 25 个标签。合并拆分时这两张必须作为同一患者 group。
- 对旧 353 个已选患者目录重新执行“PNG/JSON 同 stem 严格配对 + 每张图独立检查”后，只有 358 对；其中 1 对缺共同 18 类，另有 1 组完全重复，最终得到 356 张当前可直接使用的唯一完整图像。旧转换器产出的 368 张图像里，有 10 张 PNG 根本没有同 stem JSON，却被套用了同目录第一份标注，属于潜在错误标签。
- 因此推荐的大数据集保守规模是旧 356 + 新 700 = 1,056 张唯一、共同 18 类完整图像；若修复旧 1 例和新 6 例，可扩展到最多约 1,063 张。
- 数据目录可访问，主体位于 `labelme-export/P202607076308/`。
- 排除 macOS `._*` AppleDouble 辅助文件后，共有 707 个 JSON、706 个 PNG、1 个 XLSX，合计约 2.206 GB。
- 同时存在 1,415 个 `._*` 辅助文件；它们不应进入训练集或标注解析流程。
- 首个抽样 JSON 是 LabelMe `2025.7.4.0` 格式，`shapes` 为椎体类别（如 C2-C7）的 polygon 标注；需要继续全量核查标签集合与几何合法性。
- JSON 比 PNG 多 1 个，可能由目录中的 XLSX 对应同名 JSON、孤立 JSON 或其他命名情况导致，需按 stem 精确核查。
- 仓库已有的 `1-check_data/analyze_dataset.py` 面向“每个样本一个子目录”的旧数据组织，并依赖预设 `label.txt`；本批次主体是单层平铺的 PNG/JSON 配对，不能直接套用旧脚本。
- 本次将新增一个通用的平铺 LabelMe 扫描脚本，输出机器可读报告，重点覆盖配对、JSON 字段、图像尺寸、标签/shape 分布、边界、退化几何、重复文件及命名风险。
- 环境未安装 Pillow；本批次图像是 PNG，因此脚本将直接读取 PNG IHDR 获取尺寸/位深/颜色类型，避免为只读统计新增依赖。视觉抽样可使用系统 `sips`/FFmpeg 生成预览。
- 现有 `datasets/LAT202511/label.txt` 只有 21 个标签，含 C7、T1-T13、L1-L5、S1、CFH，但不含抽样 JSON 已出现的 C2-C6，不能作为本批次的完整标签真值。
- `measurements.xlsx` 含两个工作表（XML 体量约 425 KB 和 455 KB），看起来是批量测量结果；先完成 LabelMe 本体统计，再决定是否用于交叉核验。
- 首轮全量扫描确认主体目录内是 706 对完整配对的 PNG/JSON，706 个 JSON 均可解析，图像头均有效，且没有完全重复的图像文件。
- 初步标签结构为 24 个椎体/骶骨标签（C2-C7、T1-T12、L1-L5、S1）加股骨头标记；股骨头存在两套互斥体系：239 例使用单点 `CFH`，459 例使用双点 `FH-1`/`FH-2`，另有 8 例没有股骨头标记。
- 首轮脚本把合法的 point/line 零面积误计为退化/微小 shape，且明细上限会让计数失真；需要修复后重跑，当前的“200 个退化/微小”不能作为数据问题结论。
- 修复后全量审计结果：706 张 PNG 与 706 个 JSON 按同目录同 stem 100% 配对；706 个 JSON 全部可解析，706 张 PNG 头均有效；没有空标注、空标签、非法点、点数不足、退化面、重复标签、尺寸不一致、`imagePath` 不一致或完全重复图像。
- 共 18,058 个 shapes：16,199 个 polygon（椎体）、1,157 个 point（股骨头相关）、702 个 line（S1）；每图 15-26 个标注，中位数 26，均值 25.58。
- 几何扫描发现 1 个明确越界标注：`FQSY2505P001483_LAT_141847.json` 的 C2 有两个顶点 y=-2.33 与 -0.30；另有 2 个需要视觉复核的上下顺序异常（T5/T6 轻微交叠、T1/T2 明显倒置）。
- 发现 5 例解剖序列内部缺层：分别缺 T12、L5（2 例）、T1-T3、L4；其中缺层可能是视野截断，也可能是漏标，需结合图像判断。
- 图像尺寸共有 292 种，384/706（54.4%）为 1536×4352；PNG 颜色格式混合：391 张 8-bit 灰度（color type 0），315 张 8-bit RGBA（color type 6）。训练前需统一解码通道数，并采用保持纵横比的 resize/letterbox。
- 文件名来源前缀共 17 类，最大三类为 LFPY 164、THBZ 141、LWSY 80；拆分训练/验证/测试时应按患者/来源分组，避免同源泄漏与站点偏差。
- 顶层第 707 个 JSON 是 `export-summary.json`，并非图像标注；主体样本目录实际为严格的 706 PNG + 706 LabelMe JSON 配对。
- 当前 Python 环境也没有 OpenCV、Matplotlib、scikit-image、CairoSVG 或 Wand；视觉质检将使用现有 FFmpeg/系统工具生成只读预览，不额外安装包。
- 官方导出汇总 `export-summary.json` 与独立扫描一致：requested=706、succeeded=706，空标注/未找到/覆盖重复/对象缺失/失败均为 0。
- 越界样本的标注预览显示 C2 位于图像最上缘，图像本身截断了椎体；两个顶点仅越界约 0.3-2.3 px，属于边界裁切带来的轻微坐标外溢，但转换到 YOLO 等严格归一化格式前仍应 clip 到图像范围。

## Technical Decisions
| Decision | Rationale |
|----------|-----------|
| 使用 Python 标准库/Pillow 做全量只读扫描 | 便于复现并避免依赖 LabelMe GUI |
| 按类别与标注数量分层抽样 | 比纯随机抽样更容易覆盖少数类和异常样本 |

## Issues Encountered
| Issue | Resolution |
|-------|------------|
| Python 环境没有 Pillow | 对 PNG 用标准库解析 IHDR；视觉预览使用系统图像工具 |
| 初版几何规则误报 point/line 为零面积异常，且异常列表封顶影响计数 | 仅对面状 shape 检查面积，并保留完整问题计数后重跑 |
| FFmpeg 能识别 SVG 容器但没有 SVG 解码器 | 不走 SVG 栅格化，改用 FFmpeg 图像解码 + 标准库绘线 |
| FFmpeg `tile` 在单帧输出时只产生第一格 | 改用 8 路显式输入与 `xstack` 生成问题样本拼图 |
| PPM 解析器把像素起始的空白字节值当成头分隔符 | 只消费 maximum 后的一个换行分隔符并重生成预览 |
| 新旧 1,110 张 PNG 全量 SHA-256 在 30 秒内未完成 | 改用文件大小交集预筛候选，再只哈希可能重复的文件 |
| 初次 split 重叠检查使用 zsh 特殊变量 `path`，导致后续命令无法找到 | 改为临时 Python 脚本解析文件名中的患者标识 |
| 股骨头分布核对误读 `._*.json` | 过滤 AppleDouble 后再解析真实 JSON |
| 临时 `jq` 路径查询命中了空节点 | 查明审计文件实际字段为 `annotations.label_stats` 后改用正确路径 |
| 用 `xargs jq | head` 抽样时上游收到SIGPIPE | 已取得目标样本；后续避免在xargs长管道末端用head提前关闭输出 |

## 第一批侧面人工复核可视化（进行中）

- 已定位此前正面的人工复核包：E盘中采用逐张JPG、CSV索引、离线HTML浏览页、说明/报告和manifest的完整结构，不是只输出散图。
- 正面异常复核图使用“全图 + 局部放大”双栏；全量模型核验使用并排对照图；黑边核验图在图像上下加入文件名、风险说明和量化信息。侧面补标复核最适合合并这些模式：全图显示整条脊柱，右栏放大C2-C7，顶部显示缺失类别与high/medium/low数量。
- 颜色需要明确区分来源：原始人工polygon用青色细线；模型补标high/medium/low分别用绿色、橙色、红色粗线；未生成候选的C2-C6在红色风险横幅中列出。所有颜色含义同时写入每张图与HTML页面。
- 12张真实样本试渲染已通过完整性审计，并抽看仅high、缺候选和含low三类：全图/颈椎放大布局清晰，中文字体、颜色、四点编号和风险横幅均可读；缺候选样本能直接显示未生成的具体类别。为便于核对全脊柱层级，正式版全图也标出全部C2-L5类别名。
- 全量正式版抽看高风险样本确认复核图能够暴露严重错误：`NIU_XIAO_YUE...` 的补标类别在颈椎/胸椎间出现明显跳级和错位，图上同时列出10个无候选层级，应作为人工重画/拒绝候选的高优先级样本；`CHEN_QING_RUI...` 则能清楚看到全图中少数红色low补标与大部分high补标，局部框和四点编号可读。
- 进一步抽看严重左右黑边的medium样本`QIN_HE...`和无统计黑边的仅high样本`CERRINA_LAI...`：整图缩放、全层级文字、颈椎局部裁剪和颜色在两种画布条件下均正常；这也验证复核图保留原始黑边语境，没有在可视化阶段悄悄裁掉全图信息。
- E盘正式复核包终验通过：384行索引对应384张真实JPG，全部可解码、全部与manifest SHA-256一致，错误0；体积约393MB。风险分组为缺候选38、含low 64、含medium 43、仅high 239。另复制20张无同名JSON原图清单，明确它们未被伪造候选标注。

## Resources
- 数据目录：`/Volumes/E/spine_data/20260903-侧面数据第二批`
- 主体目录：`/Volumes/E/spine_data/20260903-侧面数据第二批/labelme-export/P202607076308`

## 人工筛选后的20类侧面联合数据集
- 人工复核CSV共368行且覆盖368个唯一文件：accepted 301、rejected 67、未判断0。
- accepted中仍有4张经全量来源审计确定为旧标签错配，必须强制排除；最终第一批可用297张。已拒绝样本中另含10张错配及1张等价重复，因此第一批总排除71张。
- 297张最终第一批样本全部存在经人工预览接受的C2伪标签候选；C7、T1-T13、L1-L5从各自同stem原始LabelMe JSON重新转换，不能继续使用旧YOLO标签中的错配结果。
- 用户最终明确类别契约为20类：`C2、C7、T1-T13、L1-L5`，不包含C3-C6。训练可利用全部20类辅助监督，但核心评估只看C2和C7。
- 第一批297张中仅2个T13 polygon，第二批698张没有T13；联合构建时保留T13类别并把这2个患者组固定到train，避免稀有类仅落入val/test。
- 第二批可用数据为698张、697个患者组；与第一批合并后的候选总规模为995张、994个带批次命名空间的患者组，仍需经过最终几何和重复审计确认。
- 新四角算法先求去重后的严格凸包，再逐次移除局部面积损失最小的顶点直到剩4点；只有真实凸包仅3点时才用最小面积旋转矩形补足。它保证四点唯一、严格凸且不会退化成三角形，同时通常让角点仍落在原轮廓顶点上。
- 297张第一批真实转换全量通过：没有关键点裁剪、没有三角凸包回退、没有近三角形拒绝；随机覆盖C7/T4/T8/T12/L2/L5的叠图中，新四边形贴合原密集轮廓四个解剖角，未见系统性偏移。
- 第二批698张既有四点标签经同一严格凸四边形检查全部通过；从23类重映射时只删除C3-C6，保留C2/C7/T1-T12/L1-L5并为T13预留新ID 14。
- 正式联合数据集已生成于`datasets/yolo_lateral_reviewed_combined_20cls`，实体文件约3.6 GB；995张、994个患者组、18,889个对象，train/val/test分别796/99/100张。
- 总类别对象数：C2 988、C7 993；T1-T3各993，T4/T7/T8各994，T5/T6/T9-T12及L1-L5各995，T13仅2。val/test分别有C2 98/99、C7 98/99，可用于本轮核心指标。
- 最终独立审计确认图像/标签一一配对，坐标均在[0,1]，每个对象四点唯一且严格凸；患者跨split为0，AppleDouble为0，14张确定错配和1张等价重复残留均为0。
- 训练入口默认使用用户刚拷回的第二批23类YOLO11l Pose最佳权重（约51 MB）做迁移，保持1280、batch 2、200 epochs；Mosaic/MixUp/Copy-Paste关闭，水平翻转0.5且左右角点同步交换。训练完成后另用test的`classes=[0,1]`只计算C2/C7重点指标。
- `datasets/`受gitignore管理，联合数据集不会随Git提交；上传AutoDL必须单独复制3.6 GB数据目录和51 MB迁移权重。

## 联合数据集四角点定向可视化
- 对最终第一批297张中的5,341个C7-T13/L1-L5密集polygon完整重放旧算法：旧结果边比<0.05有7个、<0.10有49个、<0.15有92个；最终新标签边比<0.05为0。
- 风险最高20张不同图像的旧边比范围为0.030536-0.076195，而对应新边比最低为0.501211，说明不是轻微数值改善，而是从扁塌近三角形恢复到稳定四边形。
- 总览视觉检查显示20个橙色旧结果普遍有两个点靠得很近并以一条对角线横穿椎体；绿色新结果沿青色人工轮廓四个方向分布，没有同类塌陷。
- 放大检查最严重的HOU_YU_HANG T11：旧边比0.0305，左侧两个点几乎重合，形状肉眼即为三角形；新边比0.6402，四点分别落在轮廓四角且编号顺序连续。
- 4张第一批普通对照中，旧/新算法本来都呈四边形；新算法仍贴合轮廓且未引入异常，说明修复没有只针对极端样本而破坏正常样本。
- 4张第二批直接四点对照覆盖C2、C7、T6、L5，并同时显示全片定位和局部放大；四点在颈胸腰椎不同尺度下均清楚、顺序一致。
- 放大检查第三严重的QIN_MENG_YAO L5：旧边比0.0356，左侧点几乎重叠，橙线形成明显三角形；最终绿色四点边比0.8142，完整覆盖轮廓四角。
- 最终纳入样本中旧算法没有四点坐标完全相同的对象；肉眼所见“三角形”来自两个不同点距离过近。旧算法全量最小边比0.0305，新算法全量最小边比0.2822，且新结果低于0.25的对象为0。
- 扩展包完整性终验：28行索引对应28张1680×900原尺寸JPG，20风险+4第一批普通+4第二批直接四点；HTML引用缺失0、解码/尺寸错误0、AppleDouble 0，另有1200×3220风险总览和1200×1288对照总览，包约11 MB。

## 侧面20类train黑边与ROI范围
- 对最终train 796张按近黑阈值5、边缘行98%像素为近黑、单侧黑带≥1%判“明显”重新扫描：任意连续黑边268张、明显黑边185张、同轴两侧合计≥10%的严重黑边138张。
- 第一批train 235张中明显黑边148张、严重117张，几乎都是左右黑边（左111、右148）；第二批train 561张中明显37张、严重21张，几乎都是上下黑边（上下各36张）。
- 最严重代表包括第二批LFDS2509P000160上下各约16.4%-16.5%、第一批QIN_HE左右各约14.5%-14.8%、第一批ZHANG_HAN_WEN右侧约27.3%。
- 侧面全脊柱模型不能复用骨盆“围绕单个目标缩放”的ROI定义；应只剥离检测到的连续黑带，并在剩余完整视野内同步变换每个椎体的bbox和四关键点。
- ROI只由train派生并与原始train并列输入；val/test完全不生成ROI、不改变图像或标签，患者split沿用联合数据集manifest。
- 多目标ROI构建器dry-run成功：796张train中精确选择185张明显黑边图，第一批148、第二批37；混合训练视图将为981张。裁后保留原面积比例最小67.10%、中位84.39%、最大98.84%。
- 6张最小保留面积样本的原图/ROI并排视觉检查通过：第二批主要剥离大块上下黑边，第一批主要剥离左右黑边；绿色四边形在裁剪换算后仍贴合原椎体，没有切掉任何已标椎体。
- 最极端样本`LFDS2509P000160_LAT_161147`从1536×4352裁为1536×2920（crop box 0,713,1536,3633），仍完整保留头部至骨盆的全脊柱视野；被移除的只是上下连续黑色留白。
- 部分片内仍保留标尺或非连续暗区，这是预期行为：本次ROI只删除可可靠识别的连续黑边，不做可能误伤解剖内容的内容感知紧裁。
- 正式ROI目录约1.0 GB，包含185张PNG、185份YOLO Pose标签及manifest/报告；独立`audit-only`确认185个来源患者均来自train，与val/test原图精确哈希重合为0，错误列表为空。
- 默认混合训练全量校验为train 981视图/18,637对象（796原图+185 ROI）、val 99/1,876对象、test 100/1,892对象；val/test没有ROI视图，患者跨split仍为0。
- 构建器4项测试、混合训练解析器4项测试、Shell语法和真实数据`--standard --dry-run`全部通过。

## 23类脊柱与3点骨盆双模型test可视化
- 两个用户回传的最佳权重均已定位且各约51 MB：`3-model_training/runs/pose/yolo11l_lateral_23cls_best/weights/best.pt`与`4-model_training_CFH/runs/yolo11l_pelvis_3kpt_roi_mixed_best/weights/best.pt`；两者训练记录均为YOLO11l Pose、imgsz 1280、300 epochs。
- 共同输入采用三点联合数据集的原始test split，共102张且无ROI：旧批33张、新批69张；CFH来源为直接标注60张、FH双点中点42张。这样三点结果有GT可对照，23类模型也可直接在相同侧位原图上推理。
- 三点模型语义是一个`pelvis`对象的3个关键点（CFH、S1_left、S1_right），不是3个检测类别；联合图应把它与23类椎体四角预测用不同颜色/图例呈现。
- 当前系统`python3`没有torch、ultralytics、OpenCV或Pillow，不能直接加载`.pt`推理；三点模型目录已有约85 KB的`predictions.json`，需要先核对是否覆盖完整test及其坐标格式，并继续寻找现有可用推理环境或23类预测缓存。
- 三点`predictions.json`虽包含102个唯一图像、199个检测，但与本地test文件名交集为0，因此它不是目标test缓存（很可能是训练时val导出）；不能挪用为test结果。其每图检测数为1-5不等，也需要联合可视化时按最高置信pelvis实例取舍或把多候选明确标出。
- 本机另有`/opt/miniconda3/bin/python3`可用入口，下一步检查其环境是否已有Torch/Ultralytics；若没有，再在隔离环境安装推理依赖，避免污染项目数据和系统Python。
- 第一批23类旧缓存`raw_predictions.jsonl`覆盖384张第一批候选标注图，不是当前102张三点test的完整共同缓存，尤其不能替代69张第二批test的真实推理。
- `/opt/miniconda3`已有Torch 2.8、Ultralytics 8.3.232、OpenCV 4.12和Pillow 11.3，可直接加载两个回传权重；当前沙箱内MPS不可用，但CPU单张1280推理实测23类约0.35秒、三点约0.30秒，完整102张双模型推理可行。
- 权重结构实测正确：脊柱模型names为C2-C7、T1-T12、L1-L5共23类且`kpt_shape=[4,3]`；骨盆模型为单个`pelvis`类且`kpt_shape=[3,3]`。首张test分别输出23个椎体与1个pelvis，证明输入格式兼容。
- 可视化口径固定为同图双模型独立推理：23类预测每类保留最高置信实例并显示缺类/重复候选，3点模型保留最高置信pelvis实例；同时叠加三点GT并计算CFH/S1两端点误差，便于区分“看起来合理”和真实定位误差。
- 3张小样本正式链路通过：两个权重均在1280输入上推理，3张预览/索引/JSONL一一对应且包审计无错误；4项纯逻辑测试通过。
- 两张实际预览视觉检查确认版式可读：左侧全片能连续查看C2-L5绿色四边形与类别置信度，右上放大C2-C7，右下放大骨盆；三点预测彩色实心点与白色GT圈/十字可清楚区分，S1端点和CFH的像素误差直接显示在标题区。
- 小样本中23类均有预测且解剖顺序连续，三点预测贴近GT；低阈值产生的额外同类脊柱候选不会叠加污染主图，只作为“重复候选”风险文字保留。
- 102张正式推理完成且独立审计通过：预览102、CSV索引102、JSONL预测102，引用/解码/尺寸错误0，包约26 MB。构成为旧批33、新批69。
- 三点模型在0.10阈值下没有漏检，8张出现额外pelvis候选；全体最大三点误差的中位数为图像对角线0.762%，p90约1.995%，最大4.275%。旧批中位1.327%，明显高于新批0.638%，提示三点模型仍有批次域差异。
- 23类模型80/102张检全23类，22张至少缺一类；缺失最多的是L3 12次、L5 11次、T11/L4各10次、T12 9次。高风险排序前五全部来自旧批，符合23类模型只用第二批训练后在第一批发生域偏移的预期。
- 69张被列为风险、33张为常规；风险定义偏敏感，会把0.10阈值以上的额外同类候选也列出，但主图只画每类最高置信结果，避免重复线条妨碍人工观察。
- 正式包风险第一名`SHAO_YI_JIN...`视觉抽查确认风险排序有效：23类模型只保留11/23类且在胸腰段出现大段漏检，CFH预测相对GT偏移134.4 px；页面能把第二批模型在旧批上的域失败直观暴露出来。
- 常规新批`LFPY2509P003574...`视觉抽查显示C2-L5依次完整、绿色四边形贴合各椎体，三点预测与GT邻近且骨盆放大清晰；说明同一版式既能显示严重失败，也不会把正常样本渲染成假异常。
- 两轮共4张视觉抽查覆盖新批小样本、旧批最高风险和新批常规；全图、颈椎放大、骨盆放大、颜色图例和中文状态栏均可读，未见坐标缩放或裁剪映射错误。

## Visual/Browser Findings
- 问题样本拼图覆盖 1 个越界、5 个缺层和 2 个上下顺序异常；整体上椎体四点 polygon 沿脊柱排列，标注框大小与椎体尺度匹配，未见大范围整体错位。
- 5 个“内部缺层”样本在相邻已有标注之间并不都能看到一个明显完全漏掉的未标 polygon，因此不能机械补标签：可能是编号跳跃、先天/过渡椎变异或某级标错。它们应进入人工复核队列，并结合全脊柱计数或原始影像报告判定。
- `FZBW2506P000132_LAT_100221` 中 T11 后直接到 L1，标注间距连续；更像编号/解剖变异问题，而非单纯漏画一个 T12 polygon。
- 两套股骨头标注在预览中位置均落在骨盆/股骨头附近；`FH-1`/`FH-2` 是两个单点，`CFH` 是一个中心单点，语义与训练目标不同，不能未经转换混成一个类别体系。
- `LWSY2507P000493...` 的 T5/T6 质心 y 仅倒置 0.85 px；预览显示这是脊柱弯曲造成的近水平相邻椎体，不应判为错误，可从人工问题清单降级为提示。
- `THBZ2505P003141_LAT_111109` 的 T2 明显位于 T1 上方，两个相邻 polygon 很可能标签互换，属于高优先级人工修订候选。
- 边缘缺标签并非同一种情况：`FQSY2505P001032`、`FQSY2505P001555`、`LFPY2509P003795`、`THBZ2505P003332` 等确实被图像边界截去相应区域，可合理保留为部分脊柱样本；但 `LFPY2509P000090` 与 `LFPY2509P003841` 的 C2-C6 在图像中清楚可见却完全未标，属于明显不完整标注。
- `FQSY2505P000939` 与 `LWSY2507P001049` 的图像覆盖腰骶交界但缺 S1 line，倾向于漏标；`LFPY2509P003841` 同时缺上颈椎和 S1，建议列为高优先级复核。
- 对 PZNS、GZLL、WZH、LFDS、SZSH、XTZY、PYGZ、WFSY 各抽 1 个完整样本，椎体 polygon 均沿解剖顺序连续，四角尺度与椎体匹配，未见系统性平移/缩放错误；但黑边、裁切范围、对比度和体位差异明显，来源域偏差确实存在。
- 与仓库现有流水线不直接兼容：`convert_to_yolo_pose.py` 的 class map 只有 18 个椎体（C7、T1-T12、L1-L5），会静默丢弃新数据中的 C2-C6；`S1` 本批次是 line，也被该脚本显式排除。
- 现有 `convert_to_yolo_cfh.py` 与 CFH detection 转换只识别标签 `CFH`，因此直接跑本批次只能利用 239/706（33.9%）样本；459 个 FH 双点和 8 个无股骨头标记样本会被跳过。若要最大化利用率，应先明确 FH-1/FH-2 到 CFH 的转换规则（通常可取两点中点，但需业务确认）。
- 24 个核心标签中 T4-L3 在 706 例中覆盖率均为 100%；缺失主要发生在图像边缘：C2 缺 9、C3 缺 7，S1 缺 4。内部少见缺层为 T12 缺 1、L4 缺 1、L5 缺 2。
- 股骨头体系与来源有明显相关性：例如 FQSY 72 例中 69 例用 CFH，FZBW/GZLL/LFDS/WFSY 等几乎全用 FH 双点；THBZ、LWSY、LFPY 则有混合。这会让“标签定义差异”与“来源域差异”纠缠，训练前应统一目标定义。
- PNG 通道格式几乎完全由来源决定：FQSY/LFPY/PYGZ/THBZ 全为灰度，FZBW/GZLL/LWSY/WFSY/WZH 等全为 RGBA。若预处理没有显式统一为单通道或 RGB，模型可能学到来源特征，且不同读取库的行为会不一致。
- 690/706（97.7%）样本具备全部 24 个核心 C2-S1 标签；16 例不完整。其中 5 例是内部编号跳跃，另外 11 例只缺视野边缘标签（主要上颈椎或 S1），需要区分“合理裁切”与“漏标”。
