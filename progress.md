# Progress Log

## Session: 2026-09-04

### Phase 1: 数据发现与口径确认
- **Status:** complete
- **Started:** 2026-09-04
- Actions taken:
  - 建立分析计划，明确只读检查范围。
  - 确认数据目录可访问，定位主体目录。
  - 清点 707 个 JSON、706 个 PNG、1 个 XLSX 及 1,415 个 AppleDouble 辅助文件。
- Files created/modified:
  - `task_plan.md`（新建）
  - `findings.md`（新建）
  - `progress.md`（新建）

### Phase 2: 完整性与统计分析
- **Status:** complete
- Actions taken:
  - 新增并运行平铺 LabelMe 全量审计脚本。
  - 完成配对、JSON/PNG 有效性、标签、shape、尺寸、几何、重复文件、来源与标注体系交叉统计。
- Files created/modified:
  - `1-check_data/analyze_flat_labelme.py`（新建并修正）
  - `analysis/side_labelme_20260903/audit.json`（生成）

### Phase 3: 抽样视觉质检
- **Status:** complete
- Actions taken:
  - 新增无 Pillow/OpenCV 依赖的标注预览工具。
  - 查看全部自动异常候选、边缘缺标签样本，并按 8 个额外来源抽查完整样本。
  - 将缺失标签区分为合理视野裁切、疑似漏标和需结合解剖判断的编号跳跃。
- Files created/modified:
  - `1-check_data/render_labelme_preview.py`（新建并修正）
  - `analysis/side_labelme_20260903/*.png`（生成预览）

### Phase 4: 复核与交付
- **Status:** complete
- Actions taken:
  - 编写完整分析报告。
  - 从原始数据重新运行审计，并在忽略生成时间后确认结果逐字段一致。
  - 对 T1/T2 疑似互换样本重新生成预览，确认预览工具可复现。
- Files created/modified:
  - `docs/side_labelme_20260903_analysis.md`（新建）

## Session: 2026-09-07

### Phase 5: 新旧侧面数据兼容性核对
- **Status:** complete
- **Started:** 2026-09-07
- Actions taken:
  - 恢复既有分析上下文，准备按同一口径对比新旧侧面数据。
  - 定位旧数据 `datasets/LAT202511`，并用同一审计脚本完成全量统计。
  - 确认目录、通道、polygon 点数、股骨头体系以及 C2/S1 同名异义冲突。
  - 核对旧训练样本清单：353 个有效样本、12 个无标注、26 个不完整。
  - 发现旧数据内部 1 组完全重复 PNG；跨批次全量哈希检查超时，改用候选预筛方案。
  - 用文件大小预筛确认新旧批次无字节级重复 PNG。
  - 按共同 18 类统计，新批 700/706 可直接进入完整类别候选池；旧 353 个有效患者样本对应现有 368 张 YOLO 图像。
  - 识别旧转换器“首个 JSON 应用于目录内所有 PNG”的多图目录错配风险。
  - 审计现有 YOLO split，确认 5 个患者跨 train/val/test，需在合并时整体重拆。
  - 用旧数据 18 例 C1/C2 circle 与 CFH 的共标注验证“两股骨头中心中点≈CFH”，中位误差 5.88 px。
  - 确认新批 CFH 与 FH 双点中点的归一化位置分布一致，并识别出 705 个患者 ID，其中 1 个患者有两张片。
  - 对旧有效患者逐图严格配对，确认可直接使用的共同 18 类唯一图像为 356 张；现有 YOLO 中有 10 张疑似套错标注的无同 stem 图像。
- Files created/modified:
  - `findings.md`（追加兼容性结论）
  - `progress.md`（追加核对过程）

### Phase 6: 合并方案与风险结论
- **Status:** complete
- Actions taken:
  - 形成推荐的 18 类合并口径、数据规范、转换流程和风险控制方案。
  - 复跑旧批严格配对、新批共同类别、跨批精确重复、患者 split 泄漏和股骨头中点验证。
  - 确认保守结构完整候选规模为 1,056 张，已知 T1/T2 互换修正后可使用全部候选。
- Files created/modified:
  - `docs/lateral_dataset_merge_assessment.md`（新建）
- Files created/modified:
  - `task_plan.md`（追加阶段）
  - `findings.md`（追加需求）
  - `progress.md`（追加会话）

### Phase 7: 第二批转换规范与实现
- **Status:** complete
- **Started:** 2026-09-07
- Actions taken:
  - 确认用户要求在 `datasets/` 下实际生成第二批 YOLO 训练集，并提供 AutoDL 训练脚本。
  - 固定本轮标签范围为 23 类椎体 Pose（C2-C7、T1-T12、L1-L5），S1/CFH 分离，T13 等待新增有效病例后扩类。
  - 核对第二批 C2 polygon 697 例、C7 polygon 705 例，确认可先独立训练完整椎体模型。
  - 检查现有训练入口和 `.gitignore`：生成数据已被 Git 排除；新模型需要独立的 23 类配置与训练入口，并处理水平翻转时的关键点索引交换。
  - 新增依赖标准库的 LabelMe→YOLO Pose 转换器、8例默认复核隔离表和4项单元测试。
  - 转换器实现严格配对、患者级确定性拆分、四角点规范排序、越界裁剪、原图无损复制、manifest 与转换报告输出。

### Phase 8: 生成第二批 YOLO 数据集
- **Status:** complete
- Actions taken:
  - 运行转换器，将706对源文件构建为 `datasets/yolo_lateral_20260903_23cls`。
  - 隔离8例人工复核样本，最终纳入698张图像、697名患者；按患者拆为558 train、70 val、70 test。
  - 全量验证698对图像/标签、16,032行标注、23类ID、17字段格式、归一化坐标与关键点可见性；未发现错误或患者泄漏。

### Phase 9: AutoDL 训练入口
- **Status:** complete
- Actions taken:
  - 确认本地旧模型位于 `6-app_backend/models/corner_model.pt`，可选作迁移训练初始化权重。
  - 确认数据集不会随 Git 上传，训练说明需明确单独传输约2.0 GiB的数据目录。
  - 新增23类AutoDL训练脚本，支持官方或旧模型初始化、分辨率/batch/device等参数、断点续训与dry-run。
  - 新增训练脚本测试和完整服务器操作说明；真实数据dry-run验证通过。

### Phase 10: 完整验证与交付
- **Status:** complete
- Actions taken:
  - 最终复跑7项转换/训练测试，全部通过。
  - 最终复跑真实数据dry-run与交付文件检查，确认698张图像、697名患者和8例隔离记录完整。
  - 执行Git空白错误检查并确认未触碰用户已有的 `4-model_training_CFH/train_cfh_detection.sh` 修改及未跟踪分析产物。

### Phase 11: 联合骨盆数据规范与转换器
- **Status:** in_progress
- **Started:** 2026-09-07
- Actions taken:
  - 用户提供第一批新位置 `/Volumes/E/spine_data/LAT202511`，已确认目录可访问。
  - 复核旧批335例和新批694例具备统一三关键点所需的完整标注，原始候选合计1,029例。
  - 固定联合目标为单个pelvis对象与 `CFH、S1图像左端、S1图像右端` 三关键点。
  - 核对两批严格同stem配对和代表性几何，确认旧384对、新706对，目标point/line结构可统一。
  - 新增联合骨盆LabelMe→YOLO Pose转换器及覆盖直接CFH、FH中点、S1端点排序、去重和端到端转换的测试。
  - 首轮测试暴露Python 3.13动态导入dataclass模块需先注册`sys.modules`，已修复测试夹具并保留错误记录。
  - 首次真实生成发现旧批只纳入329例；追查确认5例同时含合法S1 line与辅助S1 circle，规则已修正为采用唯一合法line，预计最终总量由1,023增至1,028。

### Phase 12: 生成联合骨盆数据集
- **Status:** complete
- Actions taken:
  - 从旧批384对和新批706对LabelMe源文件生成 `datasets/yolo_pelvis_3kpt_all`。
  - 纳入旧批334张、新批694张，共1,028张图像、1,023个患者组；按患者拆为824 train、102 val、102 test。
  - 保留569个直接CFH和459个FH-1/FH-2中点CFH，移除1张旧批完全重复图像。
  - 独立全量检查图片/标签配对、14字段格式、类别、归一化坐标、3点可见性和患者split，错误数为0。

### Phase 13: 骨盆模型AutoDL训练入口
- **Status:** complete
- Actions taken:
  - 新增1类3关键点YOLO11 Pose训练入口，默认1280、batch 4、200 epochs，使用保守医学影像增强和正确水平翻转。
  - 支持官方 `yolo11m-pose.pt` 或上一阶段23类侧面模型 `best.pt` 初始化，以及断点续训、设备、batch、imgsz等参数。
  - 训练前全量校验14字段标签、三点可见性、图片配对、manifest计数及患者split。
  - 新增4项训练脚本测试和AutoDL说明；真实数据dry-run正确识别1,028张图像、1,023个患者组。

### Phase 14: 最终验证与交付
- **Status:** complete
- Actions taken:
  - 复跑第二批脊柱和联合骨盆的转换/训练测试共17项，全部通过。
  - 复跑联合骨盆真实数据dry-run，确认824/102/102、1,028对象、1,023患者组。
  - 检查Python语法、Git空白错误和数据忽略规则，均通过；3.7 GiB数据目录不会进入Git。
  - 确认用户已有CFH训练脚本修改和未跟踪 `analysis/` 产物未被触碰或提交。
  - 用户指出训练目录职责后，将骨盆三关键点训练入口从脊柱目录移至 `4-model_training_CFH`，同步修正测试、说明和独立依赖文件；脊柱23类入口继续保留在 `3-model_training`。
  - 进一步参考原CFH训练入口，新增骨盆专用shell启动器，提供quick/standard/best预设、脊柱权重迁移、断点续训和dry-run，同时使用脚本绝对路径避免工作目录差异。
  - 同步为第二批23类脊柱模型新增同风格shell启动器，提供三档预设、旧18类corner权重迁移、续训和dry-run，使两个训练目录的使用方式一致。

### Phase 15: 第二批图像规格与黑边统计实现
- **Status:** complete
- **Started:** 2026-09-07
- Actions taken:
  - 用户要求进一步全量统计第二批图像的分辨率、纵横比和黑边情况。
  - 决定在既有LabelMe结构审计基础上增加像素外观审计，原始数据保持只读。
  - 计划同时输出逐图CSV和汇总JSON，并用极端样本视觉复核自动黑边判定。
  - 检查本机图像能力：无Pillow/OpenCV/NumPy，但有FFmpeg；决定用FFmpeg原生解码并缩小为灰度PGM，再用标准库统计边缘像素。
  - 首次阈值校准误选AppleDouble伪PNG并被FFmpeg拒绝，已将`._*`过滤加入正式实现要求。
  - 校准12个来源代表样本后，固定近黑阈值为灰度≤5、每行/列近黑像素≥98%；单侧占比≥1%定义为明显黑边。
  - 新增全量图像外观统计脚本和4项测试，覆盖PNG头、PGM首像素边界、黑边计算、分位数与汇总逻辑，全部通过。

### Phase 16: 全量运行与视觉复核
- **Status:** complete
- Actions taken:
  - 用4个并行FFmpeg解码任务完成706/706张PNG扫描，输出逐图CSV和汇总JSON，无解码失败。
  - 得到292种分辨率、宽高比和17个来源的分层统计，并区分任何黑线、明显黑边和严重黑边。
  - 视觉查看最大上下黑边LFDS、明显上下黑边LWSY、唯一左侧黑边XTZY和无边框FQSY，自动检测与肉眼一致。
  - 补充分辨率主规格占比和黑边严重度分层；对全部315张RGBA抽取alpha通道，确认全部完全不透明。

### Phase 17: 分析报告与训练建议
- **Status:** complete
- Actions taken:
  - 编写第二批图像规格与黑边正式报告，覆盖分辨率、宽高比、通道、来源分布、黑边严重度和训练影响。
  - 建议基线训练保持原图和letterbox/rect，不拉伸；后续按来源误差决定是否开展自动裁黑边并同步关键点坐标的对照实验。
  - 在新临时目录复扫706张，CSV与JSON均和首次结果逐字节一致；复跑4项单元测试和语法检查全部通过。

## Test Results

## Session: 2026-09-08（第一批脊柱补标注）

### Phase 26: 回传模型与老数据契约核对
- **Status:** complete
- **Started:** 2026-09-08
- Actions taken:
  - 用户已拷回第二批23类侧面脊柱训练权重，现要为第一批数据补标注。
  - 确认第一批路径仍可读；开始核对新权重与旧LabelMe目录，本轮不覆盖E盘原始JSON。
  - 定位新run的best.pt，用Ultralytics实际加载确认为23类、4关键点Pose模型；训练记录的Pose mAP50-95最高0.92709。
  - 盘点384份老JSON：C3-C6完全缺失，18个C2是股骨头circle而不是颈椎，现有C7-L5 polygon必须保留。
  - 确认本机Miniconda base环境已具备torch/Ultralytics/OpenCV/Pillow，可执行小样本试标。

### Phase 27: 补标注推理器与自动测试
- **Status:** complete
- Actions taken:
  - 新增第一批23类LabelMe候选标注脚本和shell入口；源目录只读、输出已存在即拒绝、仅处理严格同stem配对。
  - 每图使用原图、保守连续黑边裁剪、以已有椎体居中的0.353全高视图，并将所有候选映射回原图坐标。
  - 新增跨视图候选聚类、已有polygon代理评分、解剖顺序检查和high/medium/low复核分级；所有新增shape均显式标记需要人工复核。
  - 6项单元测试、Python语法、shell语法、真实模型/数据dry-run和Git空白检查通过。
  - 新增完整操作与安全边界说明文档。

### Phase 28: 小样本试标与视觉验收
- **Status:** complete
- Actions taken:
  - 选取12张覆盖严重黑边、轻中黑边和无黑边的代表图，运行三视图候选生成并输出全部预览。
  - 视觉复核12张预览：大多数C2-C6候选定位正确；难例缺失类别按unresolved保留，低一致性候选显式标红。
  - 试标共新增62个polygon（high 48、medium 6、low 8），未检出5个；没有把high当成无需审核的真值。
  - 独立检查原shape前缀、符号链接、伪标签旗标和坐标范围，错误数0。

### Phase 29: 第一批全量候选标注生成
- **Status:** complete
- Actions taken:
  - 使用已验证模型在本机CPU完成384张严格配对老图的三视图推理；E盘原始PNG/JSON保持只读。
  - 输出1,942个缺失polygon候选和100个无候选项；high/medium/low分别1,547/231/164，全部要求人工复核。
  - 354张获得完整C2-C6候选，30张至少缺一个颈椎候选，6张完全需要人工画C2-C6。
  - 生成扁平 `labelme_review/`，384个文件名无冲突，可由LabelMe一次打开并连续浏览。

### Phase 30: 复核工作流与交付
- **Status:** complete
- Actions taken:
  - 新增可重复的audit-only模式，校验源shape前缀、输出JSON、坐标、伪标签旗标、符号链接、manifest和review queue统计。
  - 全量audit状态passed：384份JSON、1,942个候选、100个unresolved、2,042条复核记录，错误0。
  - 7项补标注单元测试、Python与shell语法、Git空白检查全部通过；操作文档已给出试标、全量、审计和LabelMe复核步骤。
  - 确认404张PNG中384张有严格同stem JSON，20张无配对JSON；禁止把同目录其他JSON套给这些图片。
  - 以新批几何分布设计并否决局部颈椎裁剪：局部图丢失训练时全脊柱上下文，模型几乎不输出颈椎。
  - 完成30张分层样本的原图/去黑边/训练比例三视图试验；C2-C6通常有候选，但已有C7-L5代理召回仅约59%，确定输出全部需要人工复核。

### Phase 27: 补标注推理器与自动测试
- **Status:** in_progress
- Actions taken:
  - 固定三视图候选策略：原图、连续黑边安全裁剪、按第二批中位宽高比且以已有脊柱标注居中的全高视图；所有预测映射回原始像素坐标。
  - 固定只追加缺失的目标polygon、不替换任何现有shape、不处理无同stem JSON图片，并保留逐图来源和质量分级。

## Session: 2026-09-07（第一、二批图像外观比较）

### Phase 18: 第一批图像同口径全量扫描
- **Status:** complete
- **Started:** 2026-09-07
- Actions taken:
  - 用户要求比较第一批和第二批的分辨率差异，并确认第一批是否存在黑边。
  - 恢复第二批既有统计口径，决定使用相同灰度阈值、边缘覆盖阈值和黑边严重度标准扫描第一批全部404张原始PNG。
  - 第一批按患者子目录存储，需要先让现有分析器递归发现PNG；原始数据保持只读。
  - 扩展分析器以递归发现PNG并记录相对路径，新增递归与AppleDouble过滤测试；5项测试和语法检查均通过。
  - 全量扫描404/404张第一批PNG，无解码失败，输出逐图CSV和汇总JSON。

### Phase 19: 跨批分辨率与黑边比较
- **Status:** complete
- Actions taken:
  - 第一批分辨率中位数为3006×5243.5、15.78MP；第二批为1536×4352、6.68MP，第一批明显更宽、像素量更高。
  - 第一批宽高比中位数0.560，第二批0.353；第一批画布横向显著更宽，两批图像域差异明确。
  - 同阈值下第一批249/404有明显黑边、192/404为严重黑边；第二批分别43/706和26/706。
  - 第一批黑边全部集中在左右方向：左侧187张明显、右侧247张明显，顶部/底部为0；第二批则主要是上下黑边。
  - 视觉复核QIN_HE、LA_QIAN、CHEN_QING_RUI及AN_JIA_YUE样本，确认自动检测对应真实的左右空画布/黑带；无连续边带不代表影像背景中完全没有黑色区域。

### Phase 20: 比较报告与训练建议
- **Status:** complete
- Actions taken:
  - 编写第一批与第二批分辨率、宽高比、颜色模式、黑边方向和严重度的正式比较报告。
  - 明确当前 `rect=True` 等比例训练可作为基线，但不能消除第一批左右空画布造成的有效分辨率浪费。
  - 建议先按原图训练并分批评估三关键点误差；只有确认来源差距后，再构建同步调整图片、框和关键点的裁黑边对照数据集。
  - 最终复跑5项单元测试、Python语法、404行CSV及两批关键汇总断言，全部通过。
- Files created/modified:
  - `docs/first_vs_second_lateral_image_comparison.md`（新建）
  - `1-check_data/analyze_image_appearance.py`（支持递归扫描并保留相对路径）
  - `tests/test_analyze_image_appearance.py`（新增递归发现测试）
  - `analysis/side_labelme_LAT202511/*`（本地生成并由Git忽略）

## Session: 2026-09-07（侧面三关键点ROI混合数据集）

### Phase 21: 三关键点ROI混合方案与泄漏边界
- **Status:** complete
- **Started:** 2026-09-07
- Actions taken:
  - 用户授权参照正面模型处理侧面三关键点联合数据集，并特别要求避免数据泄漏。
  - 核对正面模型真实方案：先固定患者split，仅对train生成一对一GT安全ROI；train为原图+ROI，val/test保持原图且不使用GT裁剪。
  - 初步固定侧面目标规模为824张train原图加824张派生ROI；102张val和102张test均保持原样。
  - 复核现有联合manifest和训练校验器，确认可按每条train记录建立患者、批次、源图和哈希追溯链。
  - 参照正面真实ROI面积约53%，将侧面三点ROI初始几何固定为目标框每侧外扩2倍，并保留确定性5%平移、10%尺度扰动及3%安全余量；两批使用同一规则。
  - 当前Python没有Ultralytics/Pillow；生成器决定使用标准库解析标签与PNG尺寸，并调用现有FFmpeg完成PNG裁剪。

### Phase 22: ROI生成器与自动测试
- **Status:** complete
- Actions taken:
  - 准备实现只读取train、拒绝输出覆盖、临时目录原子构建、标签同步换算及manifest逐图哈希的生成器。
  - 初版生成器和4项测试通过；真实824张dry-run暴露一个bbox变换后的y边界特例，已停止在预演阶段、未生成正式数据，进入针对性修正。
  - 定位到一例六位小数YOLO框的底边超出原图约0.0027 px；在变换前把源框像素边界安全裁到原图范围，并新增真实数值回归测试。
  - 修正后5项测试、语法检查和824张真实dry-run全部通过；预演得到旧批268、新批556、819个train患者组，未写正式数据。

### Phase 23: 生成侧面三关键点ROI训练视图
- **Status:** complete
- Actions taken:
  - 初始边距2.0的真实预演得到ROI面积中位数38.80%、P10 28.54%、P90 54.38%，无全画布ROI；正式生成前继续复核该尺度及代表样本。
  - 对边距2.25、2.5、2.75做全量预演；2.5的ROI面积中位数50.06%，最接近正面约53%的已验证范围，因此将默认值调整为2.5。
  - 视觉复核旧/新批各自最小面积与中位面积ROI共4张，均保留腰椎、骶骨、骨盆及足够上下文，未见裁掉三点目标或变成过紧局部切片。
  - 正式生成 `datasets/yolo_pelvis_3kpt_roi_views`：824张ROI、824份标签、819个train患者组，旧批268、新批556，目录约1.5GB。
  - 派生目录只存在 `images/train` 和 `labels/train`，没有val/test目录；逐图复核PNG尺寸、源/输出哈希、14字段、类别、坐标和三点可见性，错误数0。

### Phase 24: 训练入口与AutoDL说明
- **Status:** complete
- Actions taken:
  - 准备新增原图+ROI多train目录YAML，并扩展训练前校验以理解派生manifest和1648个训练视图。
  - 首轮真实混合dry-run正确识别train 1648、val 102、test 102以及819个ROI患者；新增混合单元测试因测试夹具未创建嵌套根目录失败，已定位为测试准备问题。
  - 修复测试夹具后10项相关测试通过；新增混合YAML，训练Python校验器支持多个train目录及ROI manifest，shell和Python默认改为ROI混合入口。
  - 保留 `--full-frame-only` 作为公平原图基线；混合与原图两条真实dry-run分别识别1648/102/102和824/102/102，均未启动训练。
  - 更新AutoDL说明，明确需上传原始联合数据集与1.5GB ROI派生目录，并说明派生视图不等于新增病例。

### Phase 25: 独立泄漏审计与最终交付
- **Status:** complete
- Actions taken:
  - 准备按源manifest、ROI manifest和跨split图像哈希独立复核患者与同源图像泄漏。
  - 首次追加审计功能时文档补丁锚点不匹配，补丁整体安全拒绝且没有产生半成品；改为拆分应用。
  - 新增可重复执行的 `--audit-only`：检查源患者跨split、源图跨split精确重复、ROI源split/患者/哈希、ROI与val/test精确相同以及禁建holdout派生目录。
  - 10项相关测试通过；真实全量审计状态passed，六项关键泄漏计数全部为0，报告写入ROI目录 `leakage_audit.json`。
  - 最终复跑联合转换器、ROI生成器、训练校验器共16项测试，全部通过；Python与shell语法、Git空白和数据忽略规则均通过。
  - 最终混合dry-run确认train 1,648视图、val/test各102原图、源患者1,023、ROI患者819；未启动训练。
- Files created/modified:
  - `scripts/build_pelvis_roi_views.py`
  - `tests/test_build_pelvis_roi_views.py`
  - `4-model_training_CFH/pelvis_3kpt_roi_mixed.yaml`
  - `4-model_training_CFH/train_pelvis_pose_3kpt.py`
  - `4-model_training_CFH/train_pelvis_pose_3kpt.sh`
  - `tests/test_train_pelvis_pose_3kpt.py`
  - `docs/pelvis_pose_training.md`
  - `datasets/yolo_pelvis_3kpt_roi_views/*`（本地生成并由Git忽略）

| Test | Input | Expected | Actual | Status |
|------|-------|----------|--------|--------|
| 审计脚本语法 | `python3 -m py_compile` | 无语法错误 | 通过 | ✓ |
| 全量数据复扫 | 706 对 PNG/JSON | 与交付 audit 一致 | 忽略生成时间后逐字段一致 | ✓ |
| 预览回归 | T1/T2 疑似互换样本 | 生成有效 PNG | 320×906 RGB PNG | ✓ |
| 原始数据保护 | 数据目录 | 不写入原始目录 | 所有产物均在工作区或临时目录 | ✓ |
| 旧批严格配对 | 353 个已选患者目录 | 每图同 stem JSON、18 类完整、去重 | 356 张唯一完整图像 | ✓ |
| 新批共同标签 | 706 张图像 | 统计共同 18 类完整性 | 700 完整、6 不完整 | ✓ |
| 跨批精确重复 | 404 旧 PNG + 706 新 PNG | 无重复或列出重复 | 0 组跨批字节级重复 | ✓ |
| 患者 split | 现有 368 张 YOLO 图像 | 无患者跨集合 | 发现 5 个患者跨集合，需重拆 | ⚠ |
| 股骨头中点 | 旧 C1/C2/CFH 共标 18 例 | 中点接近 CFH | 中位 5.88 px、最大 10.87 px | ✓ |
| 新转换器单元测试 | 角点排序、裁剪、患者拆分、端到端转换 | 全部通过 | 4/4通过 | ✓ |
| 第二批 YOLO 全量校验 | 698张图像与16,032个对象 | 配对、字段、ID、坐标、患者拆分合法 | 0错误、0患者泄漏 | ✓ |
| AutoDL训练dry-run | 生成的23类数据集 | 校验通过且不启动GPU | 识别698张、16,032对象 | ✓ |

## Error Log
| Timestamp | Error | Attempt | Resolution |
|-----------|-------|---------|------------|
| 2026-09-04 | `git commit` 无法创建 `.git/index.lock` | 1 | 改用受控权限，并限定暂存文件范围 |
| 2026-09-04 | Python 缺少 Pillow | 1 | 使用 PNG IHDR 与系统图像工具替代 |
| 2026-09-04 | 首轮几何统计误报 point/line 且异常明细封顶 | 1 | 修正适用 shape 类型并重跑审计 |
| 2026-09-04 | FFmpeg 无法解码 SVG | 1 | 改用 PPM 中间图与标准库绘线 |
| 2026-09-04 | `tile` 拼图只有首帧 | 1 | 使用显式多输入 `xstack` 生成完整拼图 |
| 2026-09-04 | 一个预览的 PPM 像素长度校验失败 | 1 | 识别为解析器过度跳过二进制像素中的空白字节，准备精确消费单个头分隔符 |
| 2026-09-07 | 追加新阶段时补丁锚点不存在 | 1 | 改用当前文件稳定段落重新应用补丁 |
| 2026-09-07 | 跨批次 1,110 张 PNG 全量 SHA-256 超过 30 秒 | 1 | 改按文件大小交集筛选候选后哈希 |
| 2026-09-07 | split 重叠检查破坏 zsh PATH 且 awk 语法失败 | 1 | 改用临时 Python 脚本，避免 shell 特殊变量和保留名 |
| 2026-09-07 | 股骨头分布脚本误读 AppleDouble JSON | 1 | 加入 `not name.startswith("._")` 过滤 |
| 2026-09-07 | `jq` 查询审计标签时字段路径错误返回 null | 1 | 检查顶层结构后使用 `annotations.label_stats` |
| 2026-09-07 | `xargs jq` 抽样管道被末端 `head` 提前关闭并报告SIGPIPE | 1 | 数据已正确取得；后续不再用会提前关流的同类管道 |

| 2026-09-08 | 用 command -v -a 盘点Python环境时zsh将-v解析为命令 | 1 | 改用 type -a，定位到可用的Miniconda Python |
| 2026-09-08 | 首次提交补标注脚本时沙箱拒绝创建.git/index.lock | 1 | 使用受控权限仅暂存并提交本任务4个文件 |
| 2026-09-08 | 新增audit-only后旧覆盖拒绝测试的简化Args缺少audit_only字段 | 1 | 测试夹具补充audit_only=False，生产参数解析无缺失 |

## 5-Question Reboot Check
| Question | Answer |
|----------|--------|
| Where am I? | 已完成新旧侧面数据合并评估 |
| Where am I going? | 向用户交付合并结论；如获指示再实际构建大数据集 |
| What's the goal? | 只读分析侧面 LabelMe 数据集并交付可核查结论 |
| What have I learned? | 见 `findings.md` |
| What have I done? | 已完成两批数据同口径对比、严格候选计数、去重、split 泄漏检查及合并方案 |

## Session: 2026-09-08 第一批侧面人工复核可视化

### 当前任务
- 为第一批384个已有JSON配对样本生成完整的人工复核可视化图。
- 输出到E盘独立目录，不改动原始PNG/JSON，也不覆盖候选标注数据集。
- 先对齐此前正面复核图风格，再实现侧面的原标注/模型补标分色、颈椎放大和风险提示。

### 当前状态
- Phase 31 已完成：确认正面复核包采用逐张JPG、CSV、离线HTML、说明与manifest；代表图常用全图+局部放大，适合侧面颈椎复核。
- Phase 32完成：已实现全图+颈椎放大、来源/质量分色、风险横幅、离线筛选/记录页面和完整性审计；3项测试通过，12张真实样本试渲染通过并完成三类视觉抽查。
- Phase 33进行中：准备在E盘新目录生成384张全量复核图。
- 384张真实JPG已生成；首次终验把E盘自动产生的384个`._*.jpg` AppleDouble元数据旁车误计为图像，导致预览计数768并解码失败。真实384张JPG、索引行和已列文件哈希均正常；正在修正审计过滤并清理本次输出中的旁车。
- Phase 33完成：E盘独立复核包已生成384张1780×1750 JPG、HTML逐张复核页、CSV索引、说明、manifest和20张无同名JSON原图清单；原始数据未修改。
- Phase 34完成：修正并回归测试AppleDouble过滤后终验通过，384张全部可解码且SHA-256匹配，错误0；抽看缺候选、含low、含medium、仅high和严重黑边代表图均可读。正式包约393MB。

## Session: 2026-09-08 严格C2-C6补标复核包

### 当前任务
- 用户确认第一批只允许模型补C2-C6；任何原始C7-L5缺级样本不由模型补齐，也不因缺级自动排除。
- C2已经贴到或越过图像边界的样本单独标为建议排除，供人工判断是否进入训练集。
- 重新生成E盘复核包，旧包保留且原始PNG/JSON继续只读。

### 当前状态
- 用户进一步明确：个体可能先天缺少某一级椎体，因此不按原始C7-L5缺级自动排除；384张全部交由人工接受/拒绝。
- Phase 35完成：357张原始18级齐全、27张存在原始缺级；354张有完整C2-C6候选、30张至少缺一类；366张有C2候选，其中33张C2到边界≤1.01px、48张到边界≤图像短边0.5%。这些统计只用于筛选提示。
- Phase 36进行中：让复核包只叠加C2-C6伪标签，并加入C2触边/近边筛选与人工接受/拒绝记录。
- Phase 36完成：渲染器严格合成“原始全部shape + 仅C2-C6伪标签”，HTML支持接受、拒绝、修改后接受、不确定四种结论和CSV导出；C2触边/近边及原始缺级均只作筛选提示。5项测试、语法检查和12张真实试包审计通过，抽查确认没有显示C7-L5伪标签。
- Phase 37进行中：准备把384张新版完整可视化写入E盘新目录，旧复核包保留。
- Phase 37已完成生成：E盘新目录包含384张预览，首轮与独立audit-only均通过；质量优先级为仅high 249、含medium 43、含low 62、缺候选30，C2边界状态为safe 318、touch 33、near 15、missing 18，原始C7-L5缺级提示27张。
- 正式包首轮视觉终验已覆盖C2近边与C2无候选代表图，风险提示、局部放大和“仅补C2-C6”限制均正确。
- 正式包第二轮视觉终验覆盖C2触边和全高置信安全样本；共完成touch、near、missing、safe四种C2边界状态抽查，均能在图中清楚区分且不自动决定取舍。
- Phase 38完成：384张索引与预览一一唯一，新增C2-C6候选1,849个且没有其它伪标签；12项相关测试通过，独立审计的解码、哈希和类别范围错误均为0，AppleDouble旁车数为0。严格包约392MB，等待用户逐张接受/拒绝并导出CSV。

## Session: 2026-09-08 yolo_corner补标对象纠正

### 当前任务
- 用户指出需要补标和复核的是仓库 `datasets/yolo_corner`，该目录是其基于LAT构建的既有训练数据集，而不是直接对原始 `/Volumes/E/spine_data/LAT202511` 全部同stem配对进行处理。
- 已暂停后续三关键点test可视化，先核对yolo_corner的真实样本集合、split、标签和来源映射，评估上一版C2-C6复核包的偏差。

### 当前状态
- Phase 39进行中：只读检查yolo_corner，不改动现有数据或E盘复核包。
- 已确认yolo_corner为18类四角点Pose数据集，共368张（294/37/37）；此前LAT复核包与其交集358张，多26张、漏10张，范围确实不对。
- 漏掉的10张是原始LAT无同stem JSON但被旧转换器套用患者目录第一份JSON后纳入yolo_corner的图；这些现有18类标签需视为高风险，而不是简单补C2-C6后直接训练。
- Phase 39完成：前一版384张LAT复核包与yolo_corner仅358张重合，多26张、漏10张；yolo_corner真实规模为368张且实际基线是YOLO四关键点标签。
- Phase 40完成：确认前一版不能作为yolo_corner最终复核包。建议以368张现有图/YOLO标签重新渲染，只叠加C2-C6，并显式标记10张源标注错配风险、重复图和跨split患者；原yolo_corner保持只读，人工复核后再按患者重建split。
- 用户已确认按建议纠正；Phase 41开始，以yolo_corner的368张图及其实际YOLO四关键点标签为唯一复核基线。
- 已实现纠正版复核器：读取18类YOLO Pose标签转为可视化基线，358张按文件名复用既有C2-C6候选，其余样本支持惰性加载23类模型补推理；HTML增加错配、跨split、重复、基线缺级和split筛选。
- 新增2项纠正版测试并连同既有5项渲染测试通过；12张真实试包审计、解码和哈希均通过，视觉抽查确认青色基线确为yolo_corner四角点且C2触边警告正确。
- 首轮正式包已生成并审计通过：368张=358张复用+10张补推理；但补推理出现2次CPU NMS超时警告，正在用最终候选阈值0.03提前过滤后复核10张，再决定是否重建正式包。
- 10张已用0.03阈值独立复核且无NMS警告：候选类别集合全部一致，9张渲染哈希一致，CUI_YI_NING仅有亚像素坐标/1px裁剪浮动且视觉无实质差异，因此正式包无需重建；后续脚本已固定使用更稳妥阈值。
- 正式包视觉抽查源错配+缺候选样本LIANG_TIAN和基线缺级+跨split样本WANG_YANG_DI：前者明显为正位错配，应由用户拒绝；后者侧位候选可读，风险警示正确且没有自动排除。
- 第二轮正式视觉抽查覆盖精确重复WANG_YANG和test触边样本BAO_QING，重复、无候选、split和触边提示均正确。
- 使用原始LAT患者目录ID复核后，368张对应353个患者，跨split实际为4个患者/8张图；早期按姓名前缀统计的第5个YANG_YI是两个不同患者ID，已修正文档。
- Phase 41完成：E盘纠正版包含368张预览和离线接受/拒绝页面，新增C2-C6候选1,770个，29张存在至少一类无候选；独立审计确认数量、解码、哈希和类别范围错误均为0，AppleDouble旁车为0，包大小约376MB。

## Session: 2026-09-08 yolo_corner旧标签全量错配审计

### 当前任务
- 用户人工查看后发现多张旧C7-L5标注疑似与图像错配，要求全量确认数量，并将这些图像改为需要补全全部23类标注的对象。
- 审计需区分图像/JSON配对错误与同stem原始标注本身的解剖位置质量问题；原数据和现有复核包保持只读。

### 当前状态
- Phase 42进行中：准备逐图复算现有18类YOLO标签的原始JSON来源，并对无同stem JSON的多图患者做图像对应与视觉核验。
- 新增可复用审计器及单元测试，通过患者目录内所有JSON重放旧转换器，按现有YOLO数值反向识别真实标签来源。
- 首轮发现15张标签来自另一JSON；进一步核对来源PNG后，WANG_YANG一张与来源图SHA-256完全相同，应去重，其余14张需要补全23类。
- 最终全量统计：353张正确同stem、14张确定错配、1张等价重复；14张错配为train 13/val 1，涉及11位患者。368张数据集PNG与对应原始PNG哈希全部一致。
- Phase 42完成：逐图CSV、JSON汇总和14张重标名单已写入本地analysis目录，正式报告记录于docs；当前10张启发式风险复核包已标明暂停使用。

## Session: 2026-09-08 人工筛选后的侧面联合数据集重建

### 当前任务
- 读取用户刚完成的368张人工接受/拒绝结果，只保留accepted第一批样本。
- 排除旧标签错配与重复，使用稳健四角算法重建第一批旧人工密集polygon，再与第二批合并、按患者拆分并提供复训入口。

### 当前状态
- Phase 43完成：人工CSV为368/368全覆盖，accepted 301、rejected 67；再强制排除4张accepted旧标签错配后，第一批最终可用297张，且297张均有人工接受过可视化的C2候选。
- 用户已明确最终20类为C2、C7、T1-T13、L1-L5，不含C3-C6；核心指标只计算C2和C7，其余18类作为辅助监督。
- T13在第一批最终样本中仅2个对象、第二批为0；保留类别并计划将其患者组固定进train，同时在构建报告中披露稀疏性。
- Phase 44进行中：实现保证四点唯一、凸且非退化的密集polygon转换，替换旧的顺序最远点算法。
- Phase 44完成：新增20类联合构建器及6项测试；第一批297张真实数据与第二批698张标签均通过几何校验。6个真实椎体叠图抽查显示新四点贴合轮廓四角，没有三角形或整体偏移。
- Phase 45进行中：准备执行约4GB联合数据复制、患者级重拆与最终完整性审计。
- Phase 45完成：正式生成995张联合数据，第一批297、第二批698；患者级拆分796/99/100张，T13两个患者均在train。
- Phase 46完成：新增20类Python/Shell训练入口和3项训练脚本测试；两组共9项新测试、Shell语法、真实数据全量dry-run全部通过。最终审计无错配残留、无等价重复残留、无患者泄漏、无三角关键点、无AppleDouble。
- AutoDL训练说明已写入`docs/lateral_pose_20_training.md`；数据集约3.6 GB，默认迁移权重约51 MB，两者需独立上传。

### 本轮错误记录
- 一次只读的C2/T13汇总命令在用户补充类别澄清时被中断；没有产生写入。随后改用已有审计CSV和候选manifest做定向核对，得到297张均有C2候选、T13共2个的结果。
- 首次生成真实角点抽查图时人工CSV的UTF-8 BOM导致`relative_image`表头KeyError；改用`utf-8-sig`后成功，生产构建器原本已按该编码读取。

## Session: 2026-09-08 联合数据集四角点抽样可视化

### 当前任务
- 从最终20类联合数据集中抽样可视化，重点覆盖旧算法曾形成三角形/近三角形的第一批密集轮廓。
- 每个风险样本同时显示原密集轮廓、旧算法四点和新算法四点，并补充普通第一批及第二批四点对照。

### 当前状态
- Phase 47进行中：准备在最终纳入的297张第一批图上重放旧算法，以最短边/最长边和唯一点数排序，而不是随机挑选容易样本。
- 可视化脚本单测首个普通闭合矩形没有触发坐标重复；已改为重复远端坐标的狭长轮廓最小反例，保持生产算法不变。
- 首次正式渲染完成图片后在HTML字符串格式化阶段失败，原因是CSV行中的边比已序列化为字符串；异常清理已删除临时包，展示层修正为显式float后再生成。
- 首版20张最高风险包已成功生成；总览和最严重T11原尺寸图均完成视觉检查，旧近三角形与新稳定四边形对比清楚。继续补充普通第一批和第二批直接四点对照后再最终交付。
- 扩展版共28张：20张最高三角风险、4张第一批普通对照、4张第二批直接四点对照。控制组总览和第三严重L5原尺寸图已抽看，绿色最终四点均正常。
- Phase 47-48完成：28张可视化包约11 MB，索引、HTML、两个总览和28张原尺寸图全部通过解码、尺寸及引用检查；可复用脚本2项测试通过并已提交。

## Session: 2026-09-08 侧面20类黑边ROI补充

### 当前任务
- 对最终train中的带明显连续黑边图像生成去黑边ROI派生视图，追加训练而不替换原图。
- ROI严格来自train；val/test保持原始全图，所有椎体bbox和四关键点随裁剪同步变换。

### 当前状态
- 最终train 796张重新统计：检测到任意连续黑边268张，单侧达到1%的明显黑边185张，严重黑边138张。
- 第一批235张train中明显左右黑边148张；第二批561张train中明显上下黑边37张。Phase 49进行中，采用“只裁连续黑边、保留全脊柱视野”的ROI规则。
- Phase 49完成：阈值固定为近黑灰度≤5、边线近黑像素≥98%、任一连续边带≥1%；只为这185张train图生成派生ROI。ROI剥离连续黑带并受全部椎体bbox/关键点范围约束，不做骨盆式局部目标裁剪。
- Phase 50进行中：基于既有已验证的FFmpeg小图黑边检测和train-only ROI泄漏审计框架，实现20类多对象版本。
- Phase 50完成：构建器支持一张图内全部20类对象同步换算，并以全部bbox/四点约束裁剪边界；4项单测和真实796张dry-run通过，计划生成185张ROI。
- 一次多文件计划补丁因findings上下文误带乱码而整体拒绝；读取当前文件后改用精确补丁，无半成品产生。
- 混合YAML新增测试首次因嵌套source目录未创建而失败；已在测试夹具中创建目录，不修改生产解析逻辑。
- 正式ROI集已生成：185张train派生图（第一批148、第二批37），无val/test目录；与796张原图混合后train视图981张。
- 已完成6张极端样本并排抽查及最极端ROI原图检查：裁剪只去连续黑带，全脊柱和全部四角标注均保留，未见换算偏移或解剖截断。
- Phase 51进行中：执行独立audit-only、全套测试、文档更新与最终提交。
- Phase 51完成：正式ROI目录约1.0 GB，185张图像/标签一一对应；独立审计为passed、与val/test精确重复0、错误0。
- 默认训练dry-run通过：train 981视图（796原图+185 ROI）/18,637对象，val 99和test 100均保持原图；两组共8项测试及Shell语法通过。
- AutoDL说明已更新，默认`--standard`直接使用混合YAML，另保留`--full-frame-only`用于原图基线对照。

## Session: 2026-09-08 23类脊柱与3点骨盆双模型test可视化

### 当前任务
- 使用现有23类椎体最佳权重和3点骨盆最佳权重，在同一测试集合逐图独立推理并叠加展示。
- 输出可人工浏览的离线HTML包，同时保留逐图预测统计和可追溯模型/数据路径。

### 当前状态
- Phase 52进行中：先核对两个best.pt的真实路径、模型任务定义和可共同使用的test集合，再实现可视化，避免把不同split或不同关键点语义误混。
- 已确认两个best.pt均存在且约51 MB；共同test选择`yolo_pelvis_3kpt_all`的102张原始图（旧33/新69），便于三点预测与GT直接比较。
- 当前默认Python缺少YOLO推理依赖；三点模型已有预测JSON，正在核对缓存覆盖率并查找可复用的本机训练/推理环境，尚未伪造或替代23类预测。
- 已证实已有三点预测JSON不是目标test：102个唯一文件与本地test交集为0；第一批23类缓存也不覆盖共同test全量，因此正式包必须重新跑两个权重。
- Phase 52完成：两个模型头结构、共同102张test和推理环境均已验证；CPU 1280双模型单张合计约0.65秒，进入联合可视化器实现阶段。
- Phase 53完成：新增双模型联合可视化器和4项测试；3张小样本包生成/审计通过，抽看2张确认全图、颈椎放大、骨盆预测与GT对照均清晰。Phase 54开始生成102张正式包。
- 正式102张包已生成于`analysis/lateral_dual_model_test_102_20260908`，独立audit-only、4项测试和语法检查通过；包约26 MB。正在抽查风险第一名和常规样本后完成交付。
- Phase 54完成：风险第一名旧批样本和常规新批样本均已视觉终验，风险排序确实暴露旧批23类大段漏检，常规样本则完整清晰；正式包等待用户浏览。

## Session: 2026-09-09 简化20类从零训练入口

### 当前任务
- 用户明确只希望传一个`--best`，且不做迁移学习；固定为YOLO11l随机初始化、混合train、300轮、1280、batch 4。

### 当前状态
- Phase 55完成：`--best`已固定为`yolo11l-pose.yaml`随机初始化（`pretrained=false`）、混合train、300轮、1280、batch 4，不再依赖旧`.pt`权重。
- Python默认值、训练说明和测试已同步；shell语法检查、6项测试及真实数据`--best --dry-run`全部通过。
- dry-run确认混合训练集为796张原图+185张黑边ROI，共981个训练视图；val 99张、test 100张仍只使用原图，患者级拆分验证通过。
- 使用本机Ultralytics实例化模型确认scale为`l`、参数量26,230,745；`yolo11l-pose.yaml`只定义Large网络结构，混合数据由`lateral_pose_20_black_roi_mixed.yaml`决定。
